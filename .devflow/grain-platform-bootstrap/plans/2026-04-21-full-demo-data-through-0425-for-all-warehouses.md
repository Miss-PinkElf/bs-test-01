# 全仓完整演示数据补齐到 2026-04-25

## 背景（Mini Align）

- **目标**：把 `backend/src/main/resources/db/schema.sql` 的演示种子数据补齐为“所有仓库都有完整数据”，并统一把历史数据截止时间收口到 `2026-04-25`。
- **范围**：仅调整数据库初始化 SQL 与 `devflow` 过程记录，不改后端表结构、不改接口契约、不改前端页面逻辑。
- **已确认口径**：
  - 所有仓库都要有完整的温度、湿度、二氧化碳浓度数据。
  - 粮温仍以 `grain_temp_point`、`grain_temp_record`、`grain_temp_summary` 表达。
  - 3 号仓保留 `MAINTENANCE` 状态，但仍保留完整历史数据用于演示。
  - 所有演示历史数据统一截止到 `2026-04-25`。

## 问题记录（现象 / 原因 / 解决）

| 项 | 内容 |
| --- | --- |
| **现象** | 当前 `schema.sql` 中只有 1、2、6 号仓具备完整监测与预测演示数据，其余仓库只有基础档案。 |
| **原因** | 现有种子数据生成逻辑只在 `sensor_data`、`grain_temp_point`、`grain_temp_record`、`prediction_task` / `prediction_result` 中覆盖了 1、2、6 号仓。 |
| **解决** | 将种子数据配置扩展为覆盖 6 个仓库，并把时间范围统一收口到 `2026-04-25`；保留现有“稳定仓 / 风险仓 / 对比仓”故事线，同时为 3、4、5 号仓补充完整且口径一致的演示样本。 |

## 轻量任务

- [x] 新增本轮 plan 并更新活跃计划索引
- [x] 调整 `schema.sql` 的用户与数据种子，覆盖全部 6 个仓库
- [x] 将粮温、湿度、二氧化碳历史数据统一收口到 `2026-04-25`
- [x] 按现有演示口径补齐 6 个仓库的预测任务与预测结果
- [x] 执行数据库重置验证，并回写 `state.md` / `decision-log.md`
