---
phase: 04-prediction-page-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/PredictionView.vue
  - frontend/src/styles.css
autonomous: true
requirements:
  - POLISH-04-01
  - POLISH-04-02
user_setup: []

must_haves:
  truths:
    - 预测页底部仅一张「预测记录」任务表（全宽），不存在并列的「预测结果列表」分步表
    - 该表无 @row-click 绑定 selectPredictionTask；存在「操作」列含「查看摘要」「切换任务」
    - 该表存在「预测区间」列（forecast 起止），且 filteredPredictionHistory 筛选含该列展示文案
    - 列表数据来自 GET /api/predictions/tasks（前端仅 request + normalize），非页面内写死 mock
    - 点击「查看摘要」滚至任务摘要卡片并存在可验证的 prediction-summary-flash 样式
  artifacts:
    - frontend/src/views/PredictionView.vue
    - frontend/src/styles.css
  key_links:
    - backend/src/main/java/com/grain/platform/service/PredictionService.java（listTasks / predict 落库）
    - selectPredictionTask 仍承担切换任务后的图表与状态更新
---

<objective>
落实 Phase 4 **单表合并**与 **预测区间列**：底部仅保留「预测记录」任务表；增加「预测区间」列；移除原分步「预测结果列表」表；保留操作列与查看摘要强调；数据以后端 `/api/predictions/tasks` 为准并在文档中明确非 mock。
</objective>

<execution_context>
@E:/Learn/Vs/Code/bs-test-01/.cursor/get-shit-done/workflows/execute-plan.md
@E:/Learn/Vs/Code/bs-test-01/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-prediction-page-polish/04-CONTEXT.md
@.planning/phases/04-prediction-page-polish/04-UI-SPEC.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@frontend/src/views/PredictionView.vue
@frontend/src/api/grain.js
@backend/src/main/java/com/grain/platform/service/PredictionService.java
@frontend/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 单表 + 预测区间列 + 去掉分步表（D-01～D-05）</name>
  <read_first>
    - frontend/src/views/PredictionView.vue
    - .planning/phases/04-prediction-page-polish/04-CONTEXT.md
    - backend/src/main/java/com/grain/platform/service/PredictionService.java（字段来源）
  </read_first>
  <files>frontend/src/views/PredictionView.vue</files>
  <action>
    1. 移除「预测结果列表」整块（表格、分页、resultTableKeyword、filteredResultList、resultPagination 及相关 watch）。
    2. 底部仅保留一张全宽卡片，标题「预测记录」，副标题说明合并原双表语义；`el-table` 仅绑定 `historyPagination`。
    3. 列：任务号、预测对象、仓库、风险、预测天数、**预测区间**（`forecastStartTime`~`forecastEndTime`）、执行时间、固定右侧「操作」。
    4. 删除 `@row-click`；`filteredPredictionHistory` 的 filter回调含预测区间格式化字符串。
    5. 无整行点击；`.interactive-table-row` 光标为 default。
  </action>
  <acceptance_criteria>
    - 全文件无「预测结果列表」作为独立 `panel-title`（可与副标题说明文字中「原」字提及二选一，以代码结构为准：仅一张表）。
    - `rg "预测区间" frontend/src/views/PredictionView.vue` 命中列标题或模板。
    - `rg "@row-click=\"selectPredictionTask\"" frontend/src/views/PredictionView.vue` 无匹配。
    - `rg "fetchPredictionTasks|/api/predictions/tasks" frontend/src/api/grain.js` 仍命中列表数据源。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>单表与预测区间列落地；构建通过。</done>
</task>

<task type="auto">
  <name>Task 2: 查看摘要与样式（D-06）</name>
  <read_first>
    - frontend/src/views/PredictionView.vue（taskSummaryCardRef、focusTaskSummary）
    - frontend/src/styles.css（prediction-summary-flash）
  </read_first>
  <files>frontend/src/views/PredictionView.vue, frontend/src/styles.css</files>
  <action>
    1. 保持任务摘要 `el-card` 的 `ref="taskSummaryCardRef"` 与 `focusTaskSummary`（scrollIntoView center + 类名切换 + 定时移除）。
    2. 保持 `.prediction-summary-flash` 在全局 CSS 中定义，无行内样式。
  </action>
  <acceptance_criteria>
    - `rg "taskSummaryCardRef|prediction-summary-flash|focusTaskSummary" frontend/src/views/PredictionView.vue` 均命中。
    - `rg "prediction-summary-flash" frontend/src/styles.css` 命中规则块。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>查看摘要与强调样式可构建通过。</done>
</task>

<task type="auto">
  <name>Task 3: GSD 工件同步（D-07文档化）</name>
  <read_first>
    - .planning/phases/04-prediction-page-polish/04-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - .planning/phases/04-prediction-page-polish/04-CONTEXT.md
    - .planning/phases/04-prediction-page-polish/04-UI-SPEC.md
    - .planning/phases/04-prediction-page-polish/04-01-PLAN.md
    - .planning/phases/04-prediction-page-polish/04-01-SUMMARY.md
    - .planning/phases/04-prediction-page-polish/04-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </files>
  <action>
    1. 更新 CONTEXT / UI-SPEC / PLAN / SUMMARY / VERIFICATION，使「单表、预测区间、后端数据源」与代码一致。
    2. 更新 ROADMAP Phase 4 目标、阶段目录、计划勾选；REQUIREMENTS 增加 POLISH-04-* 并必要时为 UX2-* 加演进注记。
    3. STATE.md 追加一条 Phase 4 文档对齐记录（日期、摘要）。
  </action>
  <acceptance_criteria>
    - ROADMAP 中 Phase 4 指向 `.planning/phases/04-prediction-page-polish/` 且非「0 plans / TBD」占位。
    - REQUIREMENTS 含 POLISH-04-01、POLISH-04-02 条目。
  </acceptance_criteria>
  <verify>
    <automated>rg "POLISH-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>GSD 文档与实现对齐。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 浏览器 → 本页 | 管理端内页；列表与预测仍走既有 REST，不新增对外面。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-04-01 | I | `PredictionView` | 接受 | 数据源为已认证 API；无新第三方。 |
| T-04-02 | T | 文案误导 | 缓解 | 副标题说明单表含义与操作入口。 |
</threat_model>

<verification>
- `cd frontend && npm run build` 成功。
- 手工：仅一张底部表；预测区间列有值（有后端数据时）；按钮可切换任务与查看摘要。
</verification>

<success_criteria>
- CONTEXT D-01～D-07 与 POLISH-04-* 在代码与 GSD 工件中一致。
- `must_haves.truths` 可通过对代码/文档检索与一次构建验证。
</success_criteria>

<output>
执行完成后更新 `.planning/phases/04-prediction-page-polish/04-01-SUMMARY.md`
</output>

---

## PLANNING COMPLETE
