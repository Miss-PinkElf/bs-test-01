---
phase: 07-zzz-prompt-debug-origin-prompt-md
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
  - backend/src/main/java/com/grain/platform/service/GrainTempService.java
  - backend/src/main/java/com/grain/platform/mapper/GrainTempSummaryMapper.java
  - backend/src/main/resources/mapper/GrainTempSummaryMapper.xml
  - frontend/src/api/grain.js
  - frontend/src/views/DataView.vue
  - frontend/src/styles.css
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 后端存在新的粮温汇总分页接口，返回 `ApiResponse<PageResult<GrainTempSummaryItemDto>>`，并支持 `warehouseId`、`startTime`、`endTime`、`keyword`、`warningLevel`、`tempMin`、`tempMax`、`pageNum`、`pageSize`
    - `frontend/src/api/grain.js` 同时保留“汇总序列查询”和“汇总分页查询”两条调用，不把图表和表格继续绑在同一份前端内存分页数组上
    - `frontend/src/views/DataView.vue` 中粮温汇总表不再使用 `useClientPagination(filteredGrainSummaries)`，而是使用服务端分页状态
    - 粮温趋势图存在“层级目标”单选控件，图表序列为“所选目标主线 + 最高温参考线”
    - 图表和汇总表共享仓库与时间范围；关键词、预警等级、温度范围只影响汇总表，不影响趋势图
    - `mode === "env"` 仍保留原有 `fetchSensorData` / `fetchSensorTrend` 路径，未被粮温汇总新查询态破坏
  artifacts:
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/resources/mapper/GrainTempSummaryMapper.xml
    - frontend/src/api/grain.js
    - frontend/src/views/DataView.vue
  key_links:
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-RESEARCH.md
    - backend/src/main/java/com/grain/platform/dto/grain/GrainTempSummaryItemDto.java
---

<objective>
落实 Phase 7：为 `DataView.vue` 的粮温主线补齐 **汇总结果后端分页/筛选** 与 **趋势图仓库/时间范围/层级目标查询**，同时保持 env 模式与粮温原始测点记录链路不回归。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
@.planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-RESEARCH.md
@.planning/PROJECT.md
@frontend/src/views/DataView.vue
@frontend/src/api/grain.js
@backend/src/main/java/com/grain/platform/controller/GrainTempController.java
@backend/src/main/java/com/grain/platform/service/GrainTempService.java
@backend/src/main/java/com/grain/platform/mapper/GrainTempSummaryMapper.java
@backend/src/main/resources/mapper/GrainTempSummaryMapper.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 粮温汇总分页与筛选后端接口</name>
  <read_first>
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-RESEARCH.md
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/mapper/GrainTempSummaryMapper.java
    - backend/src/main/resources/mapper/GrainTempSummaryMapper.xml
    - backend/src/main/java/com/grain/platform/dto/grain/GrainTempSummaryItemDto.java
    - backend/src/main/java/com/grain/platform/common/PageResult.java
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/mapper/GrainTempSummaryMapper.java
    - backend/src/main/resources/mapper/GrainTempSummaryMapper.xml
  </files>
  <action>
    1. 保留现有 `GET /api/grain-temp/summaries` 作为图表序列接口，仅继续承载 `warehouseId`、`startTime`、`endTime`。
    2. 新增 `GET /api/grain-temp/summaries/page`，返回 `ApiResponse<PageResult<GrainTempSummaryItemDto>>`。
    3. 新接口参数必须包含：`warehouseId`、`startTime`、`endTime`、`keyword`、`warningLevel`、`tempMin`、`tempMax`、`pageNum`、`pageSize`。
    4. `GrainTempService` 参照现有 `listRecordPage` / `SensorDataService.listPage` 模式实现分页：对 `pageNum/pageSize` 做 1 和 10 的兜底，先 `countByCondition`，再 `selectPageByCondition(limit/offset)`，返回 `new PageResult<>(list, finalPageNum, finalPageSize, total)`。
    5. `GrainTempSummaryMapper.xml` 新增共享 where 片段：`warehouse_id`、时间范围、`warning_level` 精确匹配、`avg_temp` 范围；`keyword` 至少匹配 `warehouse_name`、`warning_level`、`warning_message`、`analysis_result`、`avg_temp`、`max_temp`、`min_temp` 与 `collected_at` 的字符串化结果。
    6. 排序保持 `collected_at desc, id desc` 以适配汇总表“最新在前”的分页浏览；不要改动现有 `selectByCondition` 供图表使用的按时间正序行为。
  </action>
  <acceptance_criteria>
    - `rg "/summaries/page|PageResult<GrainTempSummaryItemDto>" backend/src/main/java/com/grain/platform/controller/GrainTempController.java` 命中新增分页路由与返回类型。
    - `rg "countByCondition|selectPageByCondition" backend/src/main/java/com/grain/platform/mapper/GrainTempSummaryMapper.java` 命中新方法声明。
    - `rg "<select id=\"selectPageByCondition\"|<select id=\"countByCondition\"|warning_level|avg_temp" backend/src/main/resources/mapper/GrainTempSummaryMapper.xml` 命中新分页 SQL 与筛选条件。
    - `rg "listSummaryPage|PageResult<GrainTempSummaryItemDto>" backend/src/main/java/com/grain/platform/service/GrainTempService.java` 命中 service 分页实现。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>后端具备稳定的粮温汇总分页与筛选能力，且不破坏原序列接口。</done>
</task>

<task type="auto">
  <name>Task 2: 前端 API 与粮温汇总表服务端分页改造</name>
  <read_first>
    - frontend/src/api/grain.js
    - frontend/src/views/DataView.vue
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-RESEARCH.md
    - frontend/src/composables/useClientPagination.js
  </read_first>
  <files>
    - frontend/src/api/grain.js
    - frontend/src/views/DataView.vue
  </files>
  <action>
    1. 在 `frontend/src/api/grain.js` 中保留 `fetchGrainTempSummaries` 给图表序列使用；新增 `fetchGrainTempSummaryPage(params)`，调用 `GET /api/grain-temp/summaries/page`，返回 `normalizePageResult(raw, normalizeGrainSummary)`。
    2. `DataView.vue` 删除粮温汇总表对 `useClientPagination(filteredGrainSummaries)` 的依赖；改为独立的 `grainSummaryPageState = reactive({ pageNum: 1, pageSize: 10, total: 0 })` 与 `grainSummaryTableRows = ref([])`。
    3. 新增粮温汇总表专属筛选状态：`grainSummaryKeyword`、`grainSummaryFilters.warningLevel`、`grainSummaryFilters.tempMin`、`grainSummaryFilters.tempMax`；这些状态只驱动 `fetchGrainTempSummaryPage`。
    4. 共享查询条件统一为“仓库 + 时间范围”，并驱动两条请求：
       - 图表序列：`fetchGrainTempSummaries({ warehouseId, startTime, endTime })`
       - 表格分页：`fetchGrainTempSummaryPage({ warehouseId, startTime, endTime, keyword, warningLevel, tempMin, tempMax, pageNum, pageSize })`
    5. 汇总表分页交互使用服务端当前页/页大小/总数，不再从本地数组切片；当关键词或预警等级或温度范围变化后，必须把汇总表页码重置为 1。
    6. `latestGrainSummary` 改为基于图表序列结果或共享时间窗结果，而不是基于表格细筛后的分页结果，避免表格细筛反向影响共享提示。
  </action>
  <acceptance_criteria>
    - `rg "fetchGrainTempSummaryPage" frontend/src/api/grain.js` 命中新 API 封装。
    - `rg "useClientPagination\(filteredGrainSummaries\)|grainSummaryPagination" frontend/src/views/DataView.vue` 无命中。
    - `rg "grainSummaryPageState|grainSummaryTableRows|warningLevel|tempMin|tempMax" frontend/src/views/DataView.vue` 命中新分页态和表格筛选态。
    - 汇总表模板中的 `el-pagination` 绑定的是 `grainSummaryPageState.pageNum`、`grainSummaryPageState.pageSize`、`grainSummaryPageState.total`，而不是 `grainSummaryPagination.*`。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>汇总结果表完成服务端分页与专属细筛改造。</done>
</task>

<task type="auto">
  <name>Task 3: 趋势图共享查询与层级目标切换</name>
  <read_first>
    - frontend/src/views/DataView.vue
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - backend/src/main/java/com/grain/platform/dto/grain/GrainTempSummaryItemDto.java
  </read_first>
  <files>
    - frontend/src/views/DataView.vue
    - frontend/src/styles.css
  </files>
  <action>
    1. 在粮温主线查询区加入共享的时间范围控件，格式与现有 `value-format="YYYY-MM-DD HH:mm:ss"` 保持一致；仓库继续复用现有 `filters.warehouseId`。
    2. 在趋势图卡片增加“层级目标”单选/下拉控件，候选值至少为：`AVG_TEMP`、`LAYER_1_AVG`、`LAYER_2_AVG`、`LAYER_3_AVG`、`LAYER_4_AVG`；默认 `AVG_TEMP`。
    3. 图表渲染逻辑改为根据所选目标动态映射 y 数据：
       - `AVG_TEMP` -> `item.avgTemp`
       - `LAYER_1_AVG` -> `item.layer1Avg`
       - `LAYER_2_AVG` -> `item.layer2Avg`
       - `LAYER_3_AVG` -> `item.layer3Avg`
       - `LAYER_4_AVG` -> `item.layer4Avg`
    4. 图表始终保留第二条固定参考线 `最高温` -> `item.maxTemp`；图例文案要和当前目标同步，例如“整仓均温 / 最高温”或“一层均温 / 最高温”。
    5. 共享查询按钮触发时，同时刷新图表序列和汇总表分页；表格细筛按钮或输入变化时，只刷新汇总表分页，不重绘图表。
    6. env 模式继续走原有 `fetchSensorTrend` 和 `fetchSensorData`，不得要求 env 模式适配新的粮温层级目标状态。
  </action>
  <acceptance_criteria>
    - `rg "AVG_TEMP|LAYER_1_AVG|LAYER_2_AVG|LAYER_3_AVG|LAYER_4_AVG" frontend/src/views/DataView.vue` 命中目标选项和映射逻辑。
    - `rg "最高温" frontend/src/views/DataView.vue` 命中趋势图第二条固定参考线文案。
    - 粮温趋势图的 `series` 至少包含两条：所选目标线、最高温线。
    - `rg "fetchSensorTrend|fetchSensorData" frontend/src/views/DataView.vue` 仍命中 env 模式原路径，且未被新的粮温目标状态替换。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>趋势图完成共享查询与层级目标切换，且 env 模式不回归。</done>
</task>

<task type="auto">
  <name>Task 4: Phase 7 文档与需求追溯同步</name>
  <read_first>
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-RESEARCH.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <files>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-VERIFICATION.md
  </files>
  <action>
    1. 在 `REQUIREMENTS.md` 中为 Phase 7 新增一组明确条目（例如 `ENV-07-01` 到 `ENV-07-03`），覆盖：
       - 汇总结果后端分页 + 搜索/多条件筛选
       - 趋势图时间范围 + 仓库 + 层级目标
       - 图表与表格共享仓库/时间范围，但表格细筛不影响图表
    2. 更新 `ROADMAP.md` 中 Phase 7 的 Goal / Requirements / Plans 占位文本，不再保留 `[To be planned]` 与 `TBD`；Plans 至少落为 `07-01-PLAN.md`。
    3. 新建或更新 `07-VERIFICATION.md`，预先写明后端编译、前端构建、手工校验项（共享查询、表格分页、层级切换、env 模式回归）。
    4. 在 `STATE.md` 的 Session Notes 或等价阶段记录区追加一条 Phase 7 planning/implementation 追踪说明；如果 `gsd-tools state` 子命令因当前 STATE 格式不兼容而无法更新，则在任务总结中明确记录该限制，不要静默跳过。
  </action>
  <acceptance_criteria>
    - `rg "ENV-07" .planning/REQUIREMENTS.md` 命中新增的 Phase 7 requirement IDs。
    - `rg "07-01-PLAN.md|Phase 7" .planning/ROADMAP.md` 不再只剩 TBD 占位。
    - `.planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-VERIFICATION.md` 存在，并包含 `npm run build` 与 `mvn -q -DskipTests compile`。
  </acceptance_criteria>
  <verify>
    <automated>rg "ENV-07|07-01-PLAN.md" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 7 的 GSD 文档链路与实现目标同步。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 浏览器 → 粮温汇总分页接口 | 新增查询参数来自控制台前端，必须继续由后端做参数解析、分页兜底与 SQL 过滤。 |
| 粮温汇总序列接口 → ECharts | 图表仍只展示既有汇总字段，不新增更敏感数据。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-07-01 | T | `/api/grain-temp/summaries/page` | 缓解 | 复用现有 controller/service 参数校验，限制 `pageNum/pageSize` 最小值并在 service 内兜底。 |
| T-07-02 | D | 汇总分页查询 | 缓解 | 使用 `count + limit/offset`，避免把全量汇总继续回传到浏览器做分页。 |
| T-07-03 | I | 查询结果字段 | 接受 | 仍沿用 `GrainTempSummaryItemDto` 既有字段，不扩出新的敏感字段。 |
</threat_model>

<verification>
## 自动化

- `cd backend && mvn -q -DskipTests compile`
- `cd frontend && npm run build`
- `rg "ENV-07|07-01-PLAN.md" .planning/REQUIREMENTS.md .planning/ROADMAP.md`

## 手工建议

- 粮温主线下切换仓库与时间范围：趋势图与汇总表一起刷新
- 切换“整仓均温 / 一层 / 二层 / 三层 / 四层”：图表主线变化，最高温参考线保留
- 修改关键词 / 预警等级 / 温度范围：只刷新汇总表，不影响图表
- 分页切换后，汇总表数据来自服务端，不是前端切片
- 切换到普通环境模式后，环境趋势图和环境记录列表保持原行为
</verification>

<success_criteria>
- Phase 7 的共享查询与表格细筛边界在代码中清晰分离。
- 粮温汇总表完成服务端分页，趋势图完成层级目标切换且保留最高温参考线。
- env 模式和粮温原始测点记录链路未回归。
- Phase 7 的 REQUIREMENTS / ROADMAP / VERIFICATION 文档不再停留在 TBD 占位。
</success_criteria>

<output>
执行完成后更新 `.planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-01-SUMMARY.md`
</output>

---

## PLANNING COMPLETE
