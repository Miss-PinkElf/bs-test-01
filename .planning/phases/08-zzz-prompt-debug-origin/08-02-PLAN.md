---
phase: 08-zzz-prompt-debug-origin
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/views/BigScreenView.vue
  - frontend/src/styles.css
  - .planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SCREEN-08-01
  - SCREEN-08-02
  - SCREEN-08-03
user_setup: []

must_haves:
  truths:
    - `frontend/src/views/BigScreenView.vue` 不再导入 `mockScreenMetrics` 或 `mockScreenAlerts`，首屏先渲染标题、操作区与模块骨架，再按模块加载真实数据
    - `/screen` 继续沿用原公开路由与顶部两个动作按钮，同时新增共享 `warehouseId + timeRange` 查询控件，服务于大屏图表与明细刷新
    - 中部图表区固定存在 `粮温趋势`、`预警趋势`、`预测趋势`、`仓库对比` 4 个信息位，且粮温趋势为主图
    - 底部明细区固定存在 `最新预警`、`最近预测任务`、`重点仓库`、`说明`，其中说明块降级为次要信息，不再占主视觉
    - 图表实例在 DOM 可用后初始化，页面卸载时显式 `dispose`，单个模块数据失败时显示局部错误或空状态而不是整页白屏
  artifacts:
    - frontend/src/views/BigScreenView.vue
    - frontend/src/styles.css
    - .planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md
  key_links:
    - .planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-01-PLAN.md
    - frontend/src/layout/ConsoleLayout.vue
    - frontend/src/views/DataView.vue
---

<objective>
在真实数据底座到位后，完成 `/screen` 的三段式重构与稳定性收口：页面不再是“说明表 + 预警表”的轻量页，而是一个可稳定打开、可讲述粮温主线的答辩展示大屏。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
@.planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
@.planning/phases/08-zzz-prompt-debug-origin/08-01-PLAN.md
@.planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md
@frontend/src/views/BigScreenView.vue
@frontend/src/styles.css
@frontend/src/api/grain.js
@frontend/src/layout/ConsoleLayout.vue
@frontend/src/views/DataView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: `BigScreenView.vue` 改为分模块加载的稳定大屏外壳</name>
  <read_first>
    - frontend/src/views/BigScreenView.vue
    - frontend/src/api/grain.js
    - .planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
    - frontend/src/layout/ConsoleLayout.vue
  </read_first>
  <files>
    - frontend/src/views/BigScreenView.vue
  </files>
  <action>
    1. 删除 `mockScreenMetrics`、`mockScreenAlerts` 导入与默认渲染依赖，统一改为 `screenFilters` + `screenState` + `screenData` 三段式状态：
       - `screenFilters = reactive({ warehouseId: '', timeRange: [] })`
       - `screenState` 至少区分 `pageReady`、`loading`、`errorMessage`
       - `screenData` 至少包含 `overview`、`grainTrend`、`alertTrend`、`predictionTrend`、`warehouseComparison`、`latestPredictionTasks`
    2. 页面顶栏保留现有标题、副标题、`查看后台数据`、`返回登录` 两个动作；在顶栏下新增共享筛选区，控件固定为仓库下拉和时间范围 `datetimerange`，`value-format="YYYY-MM-DD HH:mm:ss"`，默认时间窗口用原生 `Date` 计算最近 7 天，不引入新日期库。
    3. 数据加载统一走 `fetchScreenDashboard`；页面进入后先显示总览卡骨架和模块容器，再刷新真实数据；共享筛选触发刷新时保留页面结构，不得回退为整页 loading 蒙层。
    4. 若接口异常，仅在对应模块或页头展示 `大屏数据加载失败，请先检查网络与接口状态；若仍无法恢复，使用“查看后台数据”进入管理台排查。`，同时保留 CTA 可点击。
  </action>
  <acceptance_criteria>
    - `rg "mockScreenMetrics|mockScreenAlerts" frontend/src/views/BigScreenView.vue` 无命中。
    - `rg "screenFilters|screenState|screenData|fetchScreenDashboard" frontend/src/views/BigScreenView.vue` 命中新的页面状态与加载入口。
    - `rg "查看后台数据|返回登录|value-format=\\\"YYYY-MM-DD HH:mm:ss\\\"" frontend/src/views/BigScreenView.vue` 命中保留 CTA 与共享时间范围控件。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>大屏页面已具备稳定壳层、共享筛选与真实数据加载主路径。</done>
</task>

<task type="auto">
  <name>Task 2: 中部图表区实现四块真实信息位并保证图表生命周期安全</name>
  <read_first>
    - frontend/src/views/BigScreenView.vue
    - frontend/src/views/DataView.vue
    - frontend/src/styles.css
    - .planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
  </read_first>
  <files>
    - frontend/src/views/BigScreenView.vue
    - frontend/src/styles.css
  </files>
  <action>
    1. 中部图表区固定为 4 个模块：`粮温趋势`、`预警趋势`、`预测趋势`、`仓库对比`；其中 `粮温趋势` 使用更宽的主图容器，另外 3 块做辅图。
    2. 图表数据映射固定如下：
       - `粮温趋势`：`grainTrend` 的 `primaryValue` 作为主线、`secondaryValue` 作为“最高温”参考线
       - `预警趋势`：`alertTrend` 的 `realAlertCount` / `predictionAlertCount`
       - `预测趋势`：`predictionTrend` 的 `primaryValue` / `secondaryValue`
       - `仓库对比`：`warehouseComparison` 的 `warehouseName` + `healthScore` 或 `avgTemp`
    3. 图表初始化必须在容器可用后执行：使用 `nextTick` + `requestAnimationFrame` 或等价延迟方案创建 ECharts 实例；为 4 个图表分别维护实例引用，在 `onUnmounted` 中统一 `dispose`，并在窗口尺寸变化时调用 `resize`。
    4. 图表无数据时显示 `暂无趋势数据` 或等价空状态；绝不以说明文本假装真实趋势图。
    5. 不引入自动轮播、3D、环图堆砌、全局 `el-scrollbar` 包裹或会放大白屏风险的复杂动画；首屏图表动画控制在必要范围内。
  </action>
  <acceptance_criteria>
    - `rg "粮温趋势|预警趋势|预测趋势|仓库对比" frontend/src/views/BigScreenView.vue` 命中四个图表区标题。
    - `rg "echarts.init|dispose\\(|requestAnimationFrame|resize\\(" frontend/src/views/BigScreenView.vue` 命中图表初始化、销毁与 resize 逻辑。
    - `rg "grainTrend|alertTrend|predictionTrend|warehouseComparison" frontend/src/views/BigScreenView.vue` 命中四类真实数据映射。
    - `rg "screen-main-grid|screen-chart-panel|screen-chart-primary" frontend/src/styles.css` 命中对应图表布局样式类。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>中部图表区完成真实数据渲染，且图表生命周期不会把 `/screen` 重新带回白屏/卡死风险。</done>
</task>

<task type="auto">
  <name>Task 3: 底部明细区落地最新预警、最近预测任务、重点仓库与说明</name>
  <read_first>
    - frontend/src/views/BigScreenView.vue
    - frontend/src/api/grain.js
    - .planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
  </read_first>
  <files>
    - frontend/src/views/BigScreenView.vue
    - frontend/src/styles.css
  </files>
  <action>
    1. 底部明细区固定为 4 块：
       - `最新预警`：使用 `screenData.overview.latestAlerts`
       - `最近预测任务`：使用 `screenData.latestPredictionTasks`
       - `重点仓库`：优先使用 `screenData.overview.warehouseHealthList`，必要时补 `warehouseComparison`
       - `说明`：只保留 2～3 条短句，说明数据来源与答辩用途
    2. `最新预警` 与 `最近预测任务` 默认首屏展示 5 条以内；超过后走局部滚动或紧凑列表，不让整页高度失控。
    3. 说明块文案保持次要信息位，不能占据与核心表格同等视觉面积；不再复用当前单列“展示说明”大表格形态。
    4. 底部模块为空时显示 `暂无记录` 或等价空状态，而不是保留旧的演示文本表格。
  </action>
  <acceptance_criteria>
    - `rg "最新预警|最近预测任务|重点仓库|说明" frontend/src/views/BigScreenView.vue` 命中四个底部模块标题。
    - `rg "latestPredictionTasks|latestAlerts|warehouseHealthList" frontend/src/views/BigScreenView.vue` 命中底部明细区真实数据来源。
    - `rg "展示说明" frontend/src/views/BigScreenView.vue` 无命中。
    - `rg "screen-detail-grid|screen-note-card|screen-detail-list" frontend/src/styles.css` 命中底部明细区样式类。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>底部明细区完成信息化重构，说明块被降级为次要内容。</done>
</task>

<task type="auto">
  <name>Task 4: 按 UI-SPEC 收口大屏样式与响应式节奏</name>
  <read_first>
    - .planning/phases/08-zzz-prompt-debug-origin/08-UI-SPEC.md
    - frontend/src/styles.css
    - frontend/src/views/BigScreenView.vue
    - frontend/src/layout/ConsoleLayout.vue
  </read_first>
  <files>
    - frontend/src/styles.css
    - frontend/src/views/BigScreenView.vue
  </files>
  <action>
    1. 在 `frontend/src/styles.css` 中新增大屏专属类，间距和值固定遵守 `08-UI-SPEC.md`：
       - 页面安全边距：桌面 `24px`，超宽可放大到 `32px`
       - 主分区间距：`24px`
       - 图表与明细卡片间距：`16px`
    2. 字号固定收敛到 UI-SPEC 定义：主标题 `28px/600`，分区标题 `20px/600`，正文 `16px/400`，标签与副标题 `14px/400`；不再使用当前 `40px` 指标数字与 `34px` 页面标题的历史值。
    3. 颜色继续走现有深色大屏底 + 后台面板式容器：主强调色 `#0b7a75`，预警暖色 `#d97706`，不得引入霓虹蓝、科技紫或新大屏框架风格。
    4. 响应式要求：
       - `>= 1440px`：主图 + 3 辅图 + 4 个底部模块完整铺开
       - `1100px ~ 1439px`：图表允许 2 列，底部允许换行
       - `< 1100px`：图表与明细纵向堆叠，按钮组自动换行
  </action>
  <acceptance_criteria>
    - `rg "screen-filter-bar|screen-main-grid|screen-detail-grid|screen-module-state" frontend/src/styles.css` 命中新样式块。
    - `rg "font-size: 28px|font-size: 20px|font-size: 16px|font-size: 14px" frontend/src/styles.css` 命中大屏字号收敛值。
    - `rg "#0b7a75|#d97706|#09211d" frontend/src/styles.css` 命中大屏主色、风险色与背景色。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>大屏视觉、排版和响应式行为与 `08-UI-SPEC.md` 对齐。</done>
</task>

<task type="auto">
  <name>Task 5: 实现完成后回写 Phase 8 验证与状态</name>
  <read_first>
    - .planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - .planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <action>
    1. 实现完成后，把 `08-VERIFICATION.md` 的 `status` 从 `pending` 更新为真实结果，补齐自动化与手工验证证据，明确 `/screen` 首屏稳定、图表真实化和底部明细验证结论。
    2. 在 `ROADMAP.md` 的 Phase 8 Plans 区把已完成计划勾选到对应状态，并在 Goal/Success criteria 下保持与最终实现一致的描述。
    3. 在 `STATE.md` Session Notes 追加一条 Phase 8 实施记录，至少说明：是否解决白屏/卡顿、是否落地真实接口、是否需要本地手工 UAT 继续确认。
  </action>
  <acceptance_criteria>
    - `.planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md` 存在并在执行后包含自动化与手工验证证据。
    - `rg "08-01-PLAN|08-02-PLAN" .planning/ROADMAP.md` 命中 Phase 8 计划条目。
    - `rg "Phase 8" .planning/STATE.md` 命中 Phase 8 的规划或实施记录。
  </acceptance_criteria>
  <verify>
    <automated>rg "Phase 8|08-01-PLAN|08-02-PLAN" .planning/phases/08-zzz-prompt-debug-origin/08-VERIFICATION.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>Phase 8 的实现证据和 GSD 状态链路闭环。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 公开大屏页面 → 聚合接口 | `/screen` 无登录拦截，前端必须只消费聚合与演示级明细，且失败时局部降级。 |
| ECharts 实例 → 浏览器布局系统 | 图表初始化和 resize 逻辑若处理不当，会重新触发白屏、卡顿或布局反馈循环。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-08-04 | D | `/screen` 首屏渲染 | 缓解 | 先渲染壳层和骨架，再按模块填充真实数据；接口失败只影响局部模块。 |
| T-08-05 | D | ECharts 生命周期 | 缓解 | 图表在容器稳定后初始化，窗口 resize 时显式 `resize`，组件卸载时 `dispose`。 |
| T-08-06 | I | 公开展示文案 | 缓解 | 页面只展示仓库、风险、温度、预测摘要等演示级字段，不新增账号或操作类信息。 |
</threat_model>

<verification>
## 自动化

- `cd frontend && npm run build`
- `rg "screenFilters|fetchScreenDashboard|粮温趋势|预警趋势|预测趋势|仓库对比" frontend/src/views/BigScreenView.vue`

## 手工建议

- 打开 `/screen`：确认先看到标题、按钮和骨架，而不是整页白屏
- 切换仓库与时间范围：确认四块图表与底部最近预测任务按共享条件刷新
- 模拟某类数据为空：确认对应模块显示空状态，其他模块仍可见
- 在窄屏和桌面宽屏下各看一次：确认三段式结构不溢出、不回退为旧说明表布局
</verification>

<success_criteria>
- `/screen` 完成三段式信息化重构，并优先展示真实数据。
- 首屏稳定性优先级落实，页面不再依赖 mock 数据和整页阻塞加载。
- Phase 8 的图表区、明细区、验证文档与状态记录形成可执行闭环。
</success_criteria>

<output>
执行完成后更新 `.planning/phases/08-zzz-prompt-debug-origin/08-02-SUMMARY.md`
</output>

---

## PLANNING COMPLETE
