---
phase: 10-code-comment-readability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/grain/platform/service/PredictionService.java
  - backend/src/main/java/com/grain/platform/service/DashboardService.java
  - backend/src/main/java/com/grain/platform/service/GrainTempService.java
  - backend/src/main/java/com/grain/platform/service/SensorDataImportService.java
  - backend/src/main/java/com/grain/platform/service/UserService.java
  - backend/src/main/java/com/grain/platform/service/WarehouseService.java
  - backend/src/main/java/com/grain/platform/controller/PredictionController.java
  - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
autonomous: true
requirements:
  - COMMENT-10-02
  - COMMENT-10-03
user_setup: []

must_haves:
  truths:
    - 后端关键服务与控制器能用短中文注释解释分页归一化、预测结果拼装、导入校验与汇总重算等非显而易见流程
    - 注释只落在复杂编排或校验块前，不把简单 getter/setter、DTO 透传或显而易见 controller 包装再解释一遍
    - `backend` 编译仍通过，说明本计划没有引入任何行为改动
  artifacts:
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/service/SensorDataImportService.java
    - backend/src/main/java/com/grain/platform/service/UserService.java
    - backend/src/main/java/com/grain/platform/service/WarehouseService.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
  key_links:
    - "`PredictionService.java` 仍负责把真实历史序列、预测结果与任务元数据组装成 `PredictionTaskResponse`"
    - "`GrainTempService.java` 的写操作后汇总重算链路仍通过 `rebuildSummary(...)` 统一收口"
    - "`DashboardService.java`、`UserService.java`、`WarehouseService.java` 的分页/统计边界仍与 Phase 9 契约一致"
---

<objective>
在后端高阅读成本、但当前注释偏少的关键服务与控制器中补充短中文注释，帮助维护者快速理解预测编排、分页归一化、导入校验、汇总重算与管理端统计的意图边界。

Purpose: 降低接手成本，同时严格保证接口、事务、SQL 调用时序和异常语义不变。
Output: 补充注释后的 8 个后端文件，以及可验证的 Maven 编译结果。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@backend/src/main/java/com/grain/platform/service/PredictionService.java
@backend/src/main/java/com/grain/platform/service/DashboardService.java
@backend/src/main/java/com/grain/platform/service/GrainTempService.java
@backend/src/main/java/com/grain/platform/service/SensorDataImportService.java
@backend/src/main/java/com/grain/platform/service/UserService.java
@backend/src/main/java/com/grain/platform/service/WarehouseService.java
@backend/src/main/java/com/grain/platform/controller/PredictionController.java
@backend/src/main/java/com/grain/platform/controller/GrainTempController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为预测服务编排与预测接口边界补充短注释</name>
  <read_first>
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - frontend/src/views/PredictionView.vue
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
  </files>
  <action>
    1. 在 `PredictionService.java` 中，只对 `predict(...)` 里的目标类型/数据源归一化、`listTaskPage(...)` 的页码钳制、`toResponse(...)` 的真实序列与未来预测结果拼接、`deleteTasksBatch(...)` 的整批校验流程等非显而易见块补短中文注释；不要给简单实体赋值、日志或常量定义逐行加注释。
    2. 在 `PredictionController.java` 中，只在“保留旧全量接口兼容 + 新分页接口供管理端使用”这类边界说明处补短中文注释，帮助维护者理解为什么 `/tasks` 与 `/tasks/page` 同时存在；不要把显而易见的 `ApiResponse.success(...)` 包装重复解释一遍。
    3. 注释必须是简短、直白的中文注释，只能出现在复杂编排块上方；不得改动接口路径、请求参数、日志、异常或任何运行时行为。
  </action>
  <acceptance_criteria>
    - `rg "温度指标需要先把目标类型收口成预测主线|预测分页只裁 task 主表，但返回仍组装完整任务详情|把真实序列和未来预测结果拼成同一条时间线|保留全量列表接口给旧调用兼容" backend/src/main/java/com/grain/platform/service/PredictionService.java backend/src/main/java/com/grain/platform/controller/PredictionController.java` 命中新增中文注释。
    - `rg "public PredictionTaskResponse predict|public PageResult<PredictionTaskResponse> listTaskPage|private PredictionTaskResponse toResponse|@GetMapping\\(\"/tasks/page\"\\)" backend/src/main/java/com/grain/platform/service/PredictionService.java backend/src/main/java/com/grain/platform/controller/PredictionController.java` 仍命中原有入口。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>预测主链的编排意图和接口边界已能通过短中文注释快速读懂，且没有影响现有预测功能。</done>
</task>

<task type="auto">
  <name>Task 2: 为导入校验、汇总重算与粮温接口分工补充短注释</name>
  <read_first>
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/service/SensorDataImportService.java
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - frontend/src/views/DataView.vue
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/service/SensorDataImportService.java
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
  </files>
  <action>
    1. 在 `GrainTempService.java` 中，仅对 `recordFilterOptions(...)` 的去重用途、`listRecordPage(...)` / `listSummaryPage(...)` 的参数归一化、`updateRecord(...)` 写后双向重算、`rebuildSummary(...)` 的整批汇总重建与预警判定等非显而易见块补短中文注释；不要给简单 CRUD DTO 映射或普通 return 语句加注释。
    2. 在 `SensorDataImportService.java` 中，仅对 CSV/Excel 分流、行级字段不足校验、日期单元格兼容解析等非显而易见块补短中文注释，帮助维护者理解导入失败文案对应的校验入口；不要改分支、异常文案或解析顺序。
    3. 在 `GrainTempController.java` 中，只在“汇总全量接口给图表序列、分页接口给表格”这类接口分工边界附近补短中文注释；注释必须是简短中文，且不允许任何行为改动。
  </action>
  <acceptance_criteria>
    - `rg "筛选选项只返回当前仓库已有值，避免前端再自己去重|分页查询先做关键词与页码归一化|原始记录一旦变更就重算对应时间点汇总|CSV 和 Excel 走不同解析入口，但统一产出导入行结构|汇总全量接口给图表序列，分页接口给表格" backend/src/main/java/com/grain/platform/service/GrainTempService.java backend/src/main/java/com/grain/platform/service/SensorDataImportService.java backend/src/main/java/com/grain/platform/controller/GrainTempController.java` 命中新增中文注释。
    - `rg "public PageResult<GrainTempRecordItemDto> listRecordPage|private void rebuildSummary|private List<SensorDataImportRowDto> parseCsv|@GetMapping\\(\"/summaries/page\"\\)" backend/src/main/java/com/grain/platform/service/GrainTempService.java backend/src/main/java/com/grain/platform/service/SensorDataImportService.java backend/src/main/java/com/grain/platform/controller/GrainTempController.java` 仍命中原有入口。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>导入校验、汇总重算和粮温分页/图表接口分工已有短中文注释，便于维护者理解主链而不必先通读全文件。</done>
</task>

<task type="auto">
  <name>Task 3: 为管理端分页与统计服务边界补充短注释</name>
  <read_first>
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/service/UserService.java
    - backend/src/main/java/com/grain/platform/service/WarehouseService.java
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/service/UserService.java
    - backend/src/main/java/com/grain/platform/service/WarehouseService.java
  </files>
  <action>
    1. 在 `DashboardService.java` 中，仅对“overview 只保留顶部聚合指标，三个列表走独立分页”的服务边界、分页参数兜底、模块级数据装配等非显而易见块补短中文注释；不要给简单 mapper 调用逐行加注释。
    2. 在 `UserService.java` 中，仅对关键词/页码归一化、`ensureNotLastActiveAdmin(...)` 守卫、角色集合去重与校验等非显而易见块补短中文注释，帮助维护者理解为什么这里需要集中做约束；不要改事务边界、异常语义或角色解析逻辑。
    3. 在 `WarehouseService.java` 中，仅对分页页码钳制、统计口径不依赖当前页、删除引用失败语义等非显而易见块补短中文注释；注释必须是简短中文，且不允许任何行为改动。
  </action>
  <acceptance_criteria>
    - `rg "overview 只返回顶部聚合卡片，列表模块各自分页|用户分页统一在 service 层做页码和关键词归一化|至少保留一个启用中的管理员账号|仓库统计必须走全量聚合，不能拿当前页条数代替" backend/src/main/java/com/grain/platform/service/DashboardService.java backend/src/main/java/com/grain/platform/service/UserService.java backend/src/main/java/com/grain/platform/service/WarehouseService.java` 命中新增中文注释。
    - `rg "public DashboardOverviewResponse getOverview|public PageResult<UserListItemResponse> listUsersPage|private void ensureNotLastActiveAdmin|public WarehouseStatsResponse stats" backend/src/main/java/com/grain/platform/service/DashboardService.java backend/src/main/java/com/grain/platform/service/UserService.java backend/src/main/java/com/grain/platform/service/WarehouseService.java` 仍命中原有入口。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>管理端分页与统计相关服务边界已有短中文注释，维护者能快速理解 Phase 9 收口后的后端约束。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 控制器参数 → Service 编排 | `keyword`、页码、导入文件、训练区间等仍是不可信输入；补注释时不能改变现有校验、归一化或异常分支。 |
| Service → Mapper / 其他 Service | 预测、粮温汇总、管理端统计依赖现有调用顺序；注释增强不能打乱事务边界或查询装配顺序。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | T | `PredictionService.java` / `GrainTempService.java` / `UserService.java` | mitigate | 仅允许插入短中文注释，不改条件判断、事务注解、接口签名或 mapper 调用顺序；执行后必须跑 `mvn -q -DskipTests compile`。 |
| T-10-05 | I | 后端注释文本 | mitigate | 注释只解释分页、校验和编排意图，不写数据库凭据、敏感 SQL、内部口令或环境细节。 |
| T-10-06 | D | 导入与预测服务链路 | accept | 本 phase 不新增循环和查询，也不改变批量处理规模；注释本身不提高运行负担，风险低。 |
</threat_model>

<verification>
- `cd backend && mvn -q -DskipTests compile`
- `rg "预测分页只裁 task 主表，但返回仍组装完整任务详情|原始记录一旦变更就重算对应时间点汇总|CSV 和 Excel 走不同解析入口，但统一产出导入行结构|overview 只返回顶部聚合卡片，列表模块各自分页|至少保留一个启用中的管理员账号|仓库统计必须走全量聚合，不能拿当前页条数代替" backend/src/main/java/com/grain/platform/service/PredictionService.java backend/src/main/java/com/grain/platform/service/GrainTempService.java backend/src/main/java/com/grain/platform/service/SensorDataImportService.java backend/src/main/java/com/grain/platform/service/DashboardService.java backend/src/main/java/com/grain/platform/service/UserService.java backend/src/main/java/com/grain/platform/service/WarehouseService.java`
</verification>

<success_criteria>
- 后端八个目标文件只在复杂编排、分页归一化、导入校验和汇总重算块前新增短中文注释。
- 注释覆盖预测服务编排、粮温导入/汇总、Dashboard/User/Warehouse 统计与分页边界等核心阅读痛点。
- `backend` 编译通过，且 grep 能证明注释确实加在计划指定位置。
</success_criteria>

<output>
After completion, create `.planning/phases/10-code-comment-readability/10-02-SUMMARY.md`
</output>
