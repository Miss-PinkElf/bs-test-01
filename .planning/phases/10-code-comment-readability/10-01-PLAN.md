---
phase: 10-code-comment-readability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/grain.js
  - frontend/src/views/PredictionView.vue
  - frontend/src/views/DataView.vue
  - frontend/src/views/DashboardView.vue
  - frontend/src/views/UsersView.vue
  - frontend/src/views/WarehouseView.vue
autonomous: true
requirements:
  - COMMENT-10-01
  - COMMENT-10-03
user_setup: []

must_haves:
  truths:
    - 前端关键文件能用短中文注释解释数据归一化、分页状态回源、任务切换与导入刷新等非显而易见流程
    - 注释只出现在复杂逻辑块前，不在模板区或简单赋值上重复解释显而易见代码
    - `frontend` 构建仍通过，证明本计划没有引入运行时行为改动
  artifacts:
    - frontend/src/api/grain.js
    - frontend/src/views/PredictionView.vue
    - frontend/src/views/DataView.vue
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
  key_links:
    - "`frontend/src/api/grain.js` 的 normalize* 输出形状仍与多个视图当前消费字段保持一致"
    - "`PredictionView.vue` 的历史分页、当前任务切换与删除后清理链路仍通过既有状态函数串联"
    - "`DataView.vue` 的导入成功后刷新与图表/表格查询态拆分逻辑仍保持当前时序"
---

<objective>
在前端高阅读成本、但当前注释偏少的关键文件中补充短中文注释，帮助后续维护者快速理解数据归一化、分页状态同步、任务切换、导入刷新与选中态兜底等非显而易见流程。

Purpose: 提升接手和维护效率，但严格保持现有前端行为、接口契约和页面结构不变。
Output: 补充注释后的 `grain.js` 与 5 个视图文件，以及可验证的前端 build 结果。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@frontend/src/api/grain.js
@frontend/src/views/PredictionView.vue
@frontend/src/views/DataView.vue
@frontend/src/views/DashboardView.vue
@frontend/src/views/UsersView.vue
@frontend/src/views/WarehouseView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 API 归一化与分页契约补充短注释</name>
  <read_first>
    - frontend/src/api/grain.js
    - frontend/src/views/PredictionView.vue
    - frontend/src/views/DataView.vue
    - frontend/src/views/DashboardView.vue
  </read_first>
  <files>
    - frontend/src/api/grain.js
  </files>
  <action>
    1. 仅在 `normalizeRoleCodes`、`normalizePageResult`、`normalizeOverview`、`unwrapImportResult`、`normalizePredictionTask` 这类真正承载“兼容后端返回 / 收口前端契约”语义的函数上方补充短中文注释，说明为什么需要统一结构，不要逐字段复述对象字面量。
    2. 对 `fetchDashboard*Page`、`fetchWarehousePage`、`fetchGrainTempSummaryPage`、`fetchGrainTempRecords`、`fetchPredictionTaskPage` 这一类分页请求，只在共性参数整理处补一行短中文注释，说明 `keyword/pageNum/pageSize` 默认值与空值裁剪的目的；不要在每个请求块里机械重复相同注释。
    3. 注释必须是简短、直白的中文注释，只放在非显而易见逻辑块上方；不得修改请求参数、返回结构、函数签名、字段名或任何运行时行为。
  </action>
  <acceptance_criteria>
    - `rg "统一兼容后端可能返回的角色字段|把后端分页结果收口成页面统一结构|导入接口统一按 ApiResponse\\.data 解包|预测任务列表在前端补齐 resultList 与风险字段兜底" frontend/src/api/grain.js` 命中新增中文注释。
    - `rg "function normalizeRoleCodes|function normalizePageResult|function normalizeOverview|function unwrapImportResult|function normalizePredictionTask" frontend/src/api/grain.js` 仍命中原有函数定义，证明没有改动入口名称或结构。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>`grain.js` 的高阅读成本归一化逻辑已获得短中文注释，但没有出现逐行翻译式注释泛滥。</done>
</task>

<task type="auto">
  <name>Task 2: 为预测页与数据页的状态同步链路补充短注释</name>
  <read_first>
    - frontend/src/views/PredictionView.vue
    - frontend/src/views/DataView.vue
    - frontend/src/api/grain.js
  </read_first>
  <files>
    - frontend/src/views/PredictionView.vue
    - frontend/src/views/DataView.vue
  </files>
  <action>
    1. 在 `PredictionView.vue` 中，仅对 `validateTrainRange()`、`loadPredictionHistoryPage()`、`selectPredictionTask()`、`afterDeleteRefresh()`、图表渲染前的数据分流等非显而易见块补短中文注释，解释“为什么要先重置页码 / 为什么删除当前任务时要同步清空摘要和图表 / 为什么历史列表与当前任务状态分离”；不要给简单 `ref`、表格列或模板文案加注释。
    2. 在 `DataView.vue` 中，仅对 `normalizeNumericRange()`、`loadGrainSummaryTable()`、`loadGrainRecords()`、`loadEnvData()`、导入成功后的刷新链路、grain 模式与环境模式分页状态切换等非显而易见块补短中文注释，说明“图表与表格查询态为何拆分”“导入后为什么要统一刷新哪些模块”；不要把 `el-form`、`el-table` 的显而易见模板结构注释一遍。
    3. 注释必须是短中文注释，只写在复杂逻辑块或状态同步块上方；严禁顺手重构、重排函数、改变量名、改 watch 依赖或改变任何运行时行为。
  </action>
  <acceptance_criteria>
    - `rg "训练区间校验只拦截明显无效的前端输入|历史分页刷新后尽量维持当前任务选中态|删除当前任务后同步清空摘要和图表主视觉" frontend/src/views/PredictionView.vue` 命中新增中文注释。
    - `rg "图表查询态和表格细筛态分开维护|导入成功后统一刷新图表与表格|不同模式共用分页组件时按当前主线切换状态源" frontend/src/views/DataView.vue` 命中新增中文注释。
    - `rg "validateTrainRange|loadPredictionHistoryPage|afterDeleteRefresh|normalizeNumericRange|loadGrainSummaryTable|loadEnvData" frontend/src/views/PredictionView.vue frontend/src/views/DataView.vue` 仍命中原函数名，证明只是注释增强。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>预测页与数据页的关键状态同步/刷新链路已有短中文注释，维护者无需通读整文件也能抓住核心流程。</done>
</task>

<task type="auto">
  <name>Task 3: 为仪表盘与管理页的分页/选中态逻辑补充短注释</name>
  <read_first>
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
    - frontend/src/api/grain.js
  </read_first>
  <files>
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
  </files>
  <action>
    1. 在 `DashboardView.vue` 里，只对 `createPageSection()`、`loadSection()`、关键词变更后重置页码的 `watch`、模块级错误隔离等非显而易见块补短中文注释，说明三个分页模块为何保持独立状态；不要对指标卡或简单模板循环加注释。
    2. 在 `UsersView.vue` 里，只对 `refreshUsersAndStats()`、用户分页状态回源、角色元数据与列表并行加载等逻辑块补短中文注释，解释为什么列表和顶部统计要一起刷新；不要给表单字段、按钮文案或显而易见的 CRUD 调用逐行加注释。
    3. 在 `WarehouseView.vue` 里，只对 `syncSelectedWarehouse()`、翻页后保留选中仓库快照、统计与列表并行刷新等逻辑块补短中文注释，帮助维护者理解“列表驱动详情”与“跨页兜底快照”关系；注释必须是简短中文，且不允许任何行为改动。
  </action>
  <acceptance_criteria>
    - `rg "三个分页模块各自维护独立的查询态|关键词变化时先回到第一页再回源|模块错误只留在各自卡片内部" frontend/src/views/DashboardView.vue` 命中新增中文注释。
    - `rg "列表和顶部统计共用一次刷新入口|角色元数据与用户列表首屏并行加载" frontend/src/views/UsersView.vue` 命中新增中文注释。
    - `rg "翻页后优先复用当前选中 id|当前页找不到时才退回快照或第一页" frontend/src/views/WarehouseView.vue` 命中新增中文注释。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>仪表盘、用户页、仓库页的分页与选中态逻辑都有短中文注释，但页面结构、交互和接口调用保持原样。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器输入 → 前端分页/筛选状态 | `keyword`、`pageNum`、`pageSize`、训练区间与导入刷新链路仍是现有行为边界，补注释时不能改动 `watch`、请求参数或状态回写顺序。 |
| API 归一化层 → 页面消费层 | `grain.js` 的 normalize 输出被多个页面共享，注释增强不能改变字段名、默认值或兜底逻辑。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `frontend/src/api/grain.js` 与各视图状态函数 | mitigate | 本计划只允许插入短中文注释，不改表达式、`watch` 依赖、请求参数或返回结构；执行后必须跑 `npm run build`。 |
| T-10-02 | I | 前端注释文本 | mitigate | 注释只解释状态同步和归一化意图，不写内部地址、账号、SQL 或其他敏感实现细节。 |
| T-10-03 | D | `PredictionView.vue` / `DataView.vue` 刷新链路 | accept | 本 phase 不新增请求、不改变刷新时序；注释本身不改变运行复杂度，风险低。 |
</threat_model>

<verification>
- `cd frontend && npm run build`
- `rg "统一兼容后端可能返回的角色字段|历史分页刷新后尽量维持当前任务选中态|图表查询态和表格细筛态分开维护|三个分页模块各自维护独立的查询态|翻页后优先复用当前选中 id" frontend/src/api/grain.js frontend/src/views/PredictionView.vue frontend/src/views/DataView.vue frontend/src/views/DashboardView.vue frontend/src/views/WarehouseView.vue`
</verification>

<success_criteria>
- 前端六个目标文件只在非显而易见逻辑块上方新增短中文注释。
- 注释覆盖数据归一化、分页状态回源、任务切换、导入刷新、选中态兜底等本 phase 核心阅读痛点。
- `frontend` 构建通过，且 grep 能证明注释确实加在计划指定位置。
</success_criteria>

<output>
After completion, create `.planning/phases/10-code-comment-readability/10-01-SUMMARY.md`
</output>
