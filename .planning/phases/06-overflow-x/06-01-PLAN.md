---
phase: 06-overflow-x
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles.css
  - frontend/src/views/PredictionView.vue
autonomous: true
requirements:
  - OVERFLOW-06-01
  - OVERFLOW-06-02
  - OVERFLOW-06-03
user_setup: []

must_haves:
  truths:
    - `.console-main-native` 不再一刀切 `overflow-x: hidden`，真实超宽场景可正常横向滚动
    - 主内容层仍不恢复 `el-scrollbar`，保留对 `el-scrollbar + ECharts + ResizeObserver` 反馈循环的规避
    - `PredictionView.vue` 的「预测记录」表继续由局部 wrapper 承担横向滚动
    - 任务摘要卡片与其 descriptions 结构补齐 `min-width: 0` / fixed-layout / 内容换行或局部 wrapper，避免右侧裁切
    - 前端构建通过，且 Phase 6 文档与 REQUIREMENTS / ROADMAP / STATE 同步
  artifacts:
    - frontend/src/styles.css
    - frontend/src/views/PredictionView.vue
    - .planning/phases/06-overflow-x/06-CONTEXT.md
    - .planning/phases/06-overflow-x/06-UI-SPEC.md
    - .planning/phases/06-overflow-x/06-VERIFICATION.md
  key_links:
    - .planning/quick/260410-k32-vue-scrollbar-echarts-layout-fix/260410-k32-SUMMARY.md
    - .devflow/grain-platform-bootstrap/bug-log.md
---

<objective>
收口主内容横向滚动与预测页局部 overflow-x：恢复真实超宽时的正常横向滚动能力，同时保持对主内容层 `el-scrollbar + ECharts` 反馈循环问题的规避。
</objective>

<context>
@.planning/phases/06-overflow-x/06-CONTEXT.md
@.planning/phases/06-overflow-x/06-UI-SPEC.md
@frontend/src/layout/ConsoleLayout.vue
@frontend/src/styles.css
@frontend/src/views/PredictionView.vue
@.devflow/grain-platform-bootstrap/bug-log.md
</context>

<tasks>

<task type="done">
  <name>Task 1: 主内容层 overflow-x 策略收口</name>
  <files>
    - frontend/src/styles.css
  </files>
  <action>
    将 `.console-main-native` 从全局 `overflow-x: hidden` 调整为 `overflow-x: auto`，包括小屏断点；保留主内容原生滚动模型，不恢复 `el-scrollbar`。
  </action>
  <verify>
    检查 `styles.css` 中 `.console-main-native` 为 `overflow-x: auto`
  </verify>
  <done>已完成。</done>
</task>

<task type="done">
  <name>Task 2: 预测页任务摘要局部收口</name>
  <files>
    - frontend/src/views/PredictionView.vue
    - frontend/src/styles.css
  </files>
  <action>
    为任务摘要卡片增加 `task-summary-card` / `task-summary-table-wrap` 结构，并为 card body、wrapper、`el-descriptions` 表格与内容单元格补齐 `min-width: 0`、fixed-layout、换行 / breaking，避免摘要在窄列中被裁切。
  </action>
  <verify>
    检查 `PredictionView.vue` 中 wrapper 结构；检查 `styles.css` 中 `task-summary-*` 规则
  </verify>
  <done>已完成。</done>
</task>

<task type="done">
  <name>Task 3: GSD Phase 6 文档同步</name>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/06-overflow-x/06-CONTEXT.md
    - .planning/phases/06-overflow-x/06-UI-SPEC.md
    - .planning/phases/06-overflow-x/06-VERIFICATION.md
  </files>
  <action>
    将 Phase 6 的目标、需求、验证结果与上游 quick / devflow 记录串起来，补齐 GSD 文档链路。
  </action>
  <verify>
    `ROADMAP / REQUIREMENTS / STATE` 与 phase 目录存在对应条目
  </verify>
  <done>已完成。</done>
</task>

</tasks>

<verification>
## 自动化

- `cd frontend && npm run build`

## 手工建议

- 进入 `/prediction`，确认页面不再横向持续变宽
- 宽内容真实溢出时，主内容区可出现横向滚动
- 任务摘要在右侧窄列中不再直接裁切
</verification>
