---
phase: 09-mock
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/src/main/java/com/grain/platform/controller/DashboardController.java
  - backend/src/main/java/com/grain/platform/service/DashboardService.java
  - backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java
  - backend/src/main/resources/mapper/DashboardMapper.xml
  - frontend/src/api/grain.js
  - frontend/src/views/DashboardView.vue
  - frontend/src/mock/platform.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Dashboard 顶部 overview 指标继续使用聚合接口，但下方 `近期预警`、`仓库运行健康度`、`最新粮温汇总` 改为独立分页接口
    - Dashboard 三个模块各自维护 keyword、pageNum、pageSize、loading、error，不因单模块失败导致 overview 不可见
    - `frontend/src/mock/platform.js` 被删除，运行时不再以本地 mock 数据兜底列表页面
    - Phase 9 的需求与 roadmap 追溯被同步到 planning 文档中，而不是一直停留在 `TBD`
  artifacts:
    - backend/src/main/java/com/grain/platform/controller/DashboardController.java
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/resources/mapper/DashboardMapper.xml
    - frontend/src/views/DashboardView.vue
    - frontend/src/mock/platform.js
    - .planning/REQUIREMENTS.md
  key_links:
    - .planning/phases/09-mock/09-CONTEXT.md
    - .planning/phases/09-mock/09-RESEARCH.md
    - .planning/phases/09-mock/09-UI-SPEC.md
---

<objective>
完成 Phase 9 的第二波收口：把 Dashboard 从“overview 聚合 + 前端本地分页/增量加载”改为“overview + 独立分页接口”的真实数据结构，删除残留 mock 文件，并补齐 Phase 9 的文档追溯与 requirement 标识。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-mock/09-CONTEXT.md
@.planning/phases/09-mock/09-RESEARCH.md
@.planning/phases/09-mock/09-UI-SPEC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/src/main/java/com/grain/platform/service/DashboardService.java
@backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java
@backend/src/main/resources/mapper/DashboardMapper.xml
@frontend/src/api/grain.js
@frontend/src/views/DashboardView.vue
@frontend/src/mock/platform.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard 独立分页接口</name>
  <read_first>
    - .planning/phases/09-mock/09-CONTEXT.md
    - .planning/phases/09-mock/09-RESEARCH.md
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java
    - backend/src/main/resources/mapper/DashboardMapper.xml
    - frontend/src/views/DashboardView.vue
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/controller/DashboardController.java
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java
    - backend/src/main/resources/mapper/DashboardMapper.xml
  </files>
  <action>
    1. 保留现有 `GET /api/dashboard/overview` 作为顶部指标聚合接口，不再把三个分页列表数据继续塞回 overview。
    2. 新增三个独立分页接口，命名保持清晰且与前端模块一一对应，例如：
       - `GET /api/dashboard/alerts`
       - `GET /api/dashboard/warehouse-health`
       - `GET /api/dashboard/grain-summaries`
    3. 三个接口统一支持 `keyword`、`pageNum`、`pageSize`，返回 `ApiResponse<PageResult<...>>`。
    4. `DashboardMapper.xml` 需要把当前带固定 `limit 4/6` 的查询拆成 `count + selectPage` 形式；keyword 至少覆盖当前 Dashboard 模块里用户肉眼可见的字段，不新增复杂结构化筛选。
    5. Dashboard Service 层按模块分别暴露分页查询方法，避免继续在 `getOverview()` 中组装分页列表。
  </action>
  <acceptance_criteria>
    - `rg "/alerts|/warehouse-health|/grain-summaries|PageResult" backend/src/main/java/com/grain/platform/controller/DashboardController.java` 命中新接口。
    - `rg "count.*Alert|count.*Health|count.*Summary|select.*Page" backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java backend/src/main/resources/mapper/DashboardMapper.xml` 命中分页 mapper 方法和 SQL。
    - `rg "getOverview\\(|PageResult" backend/src/main/java/com/grain/platform/service/DashboardService.java` 仍命中 overview 聚合，同时出现新的分页服务方法。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>Dashboard 后端具备 overview + 三个独立分页模块的数据接口。</done>
</task>

<task type="auto">
  <name>Task 2: DashboardView 改为 overview + 三个独立分页模块</name>
  <read_first>
    - frontend/src/views/DashboardView.vue
    - frontend/src/api/grain.js
    - .planning/phases/09-mock/09-UI-SPEC.md
    - .planning/phases/09-mock/09-RESEARCH.md
  </read_first>
  <files>
    - frontend/src/api/grain.js
    - frontend/src/views/DashboardView.vue
  </files>
  <action>
    1. 在 `frontend/src/api/grain.js` 新增 Dashboard 三个分页模块的 API 封装，并继续复用 `normalizePageResult(...)`。
    2. `DashboardView.vue` 保留顶部指标卡的 `fetchOverview()`，但把下方 `近期预警`、`仓库运行健康度`、`最新粮温汇总` 改为三个独立分页状态：各自维护 `keyword`、`pageNum`、`pageSize`、`total`、`loading`、`error`。
    3. 移除 `useClientPagination` 和 `useIncrementalList` 在 `DashboardView` 中作为真实数据主链的用法；`近期预警` 不再使用“滚到底继续加载更多预警”的增量列表模式。
    4. 保留现有后台首页布局与说明卡，但保证视觉主次关系为：overview 指标 + 三个真实分页模块优先，说明卡最弱。
    5. 某个分页模块失败时，只在该模块内显示错误提示，不影响 overview 指标卡与其他模块继续展示。
  </action>
  <acceptance_criteria>
    - `rg "useClientPagination|useIncrementalList" frontend/src/views/DashboardView.vue` 无命中或不再作为真实数据主链。
    - `rg "fetchOverview|fetchDashboard.*Page|pageNum|pageSize|total|loading|error" frontend/src/views/DashboardView.vue frontend/src/api/grain.js` 命中 overview + 三个独立分页模块状态。
    - `rg "handleAlertScroll|loadMore|visibleAlerts" frontend/src/views/DashboardView.vue` 无命中。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>DashboardView 完成从本地分页/增量加载到后端分页模块的迁移。</done>
</task>

<task type="auto">
  <name>Task 3: 删除残留 mock 与清理无效前端依赖</name>
  <read_first>
    - .planning/phases/09-mock/09-CONTEXT.md
    - frontend/src/mock/platform.js
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
    - frontend/src/views/PredictionView.vue
  </read_first>
  <files>
    - frontend/src/mock/platform.js
    - frontend/src/views/DashboardView.vue
    - frontend/src/views/UsersView.vue
    - frontend/src/views/WarehouseView.vue
    - frontend/src/views/PredictionView.vue
  </files>
  <action>
    1. 删除 `frontend/src/mock/platform.js`。
    2. 清理四个目标页面中与该 mock 文件相关的遗留引用或“演示型语义”文案，确保运行时不再依赖本地 mock 文件。
    3. 若 `useClientPagination.js` / `useIncrementalList.js` 在这四个页面中已无实际主链路引用，移除对应 import；是否保留工具文件本体由执行时根据全局剩余引用决定，本计划不要求为了零 unused 去扩大战线。
  </action>
  <acceptance_criteria>
    - `Test-Path 'frontend/src/mock/platform.js'` 返回 `False`。
    - `rg "mockUserSummaryCards|mockUsers|mockRoleProfiles|mockRecentSensorRecords|mockWarehouseHealth|mockPredictionArchives|mockScreenMetrics|mockScreenAlerts" frontend/src` 无命中。
    - `rg "演示|mock" frontend/src/views/DashboardView.vue frontend/src/views/UsersView.vue frontend/src/views/WarehouseView.vue frontend/src/views/PredictionView.vue` 不再命中与本阶段目标冲突的运行时说明文案。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>残留 mock 文件与相关运行时依赖被移除。</done>
</task>

<task type="auto">
  <name>Task 4: Phase 9 需求与状态追溯同步</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/09-mock/09-CONTEXT.md
    - .planning/phases/09-mock/09-RESEARCH.md
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <action>
    1. 在 `REQUIREMENTS.md` 中新增 Phase 9 对应 requirement IDs（例如 `LIST-09-01` ~ `LIST-09-04`），覆盖：Users/Warehouse/Prediction 服务端分页、Dashboard 独立分页接口、mock 删除、交互不回归。
    2. 更新 `ROADMAP.md` Phase 9 条目中的 Goal / Requirements / Success criteria / Plans，使其不再停留在 `TBD`。
    3. 在 `STATE.md` Session Notes 中追加 Phase 9 planning 落盘记录，说明 Phase 9 已有 `CONTEXT`、`RESEARCH`、`UI-SPEC`、`09-01-PLAN.md`、`09-02-PLAN.md`；若 `gsd-tools state` 仍因当前格式不兼容无法写入，执行总结中必须显式记录这一限制。
  </action>
  <acceptance_criteria>
    - `rg "LIST-09|Phase 9" .planning/REQUIREMENTS.md .planning/ROADMAP.md` 命中新增的 Phase 9 requirement IDs 与 roadmap 细化条目。
    - `rg "09-01-PLAN.md|09-02-PLAN.md|Phase 9" .planning/STATE.md` 命中新的 planning 状态记录。
  </acceptance_criteria>
  <verify>
    <automated>rg "LIST-09|Phase 9" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>Phase 9 的需求、路线图和状态追溯完成同步。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 浏览器 → Dashboard 分页接口 | keyword、pageNum、pageSize 来自浏览器输入，必须继续参数化查询并做分页兜底。 |
| Dashboard overview → 下方独立列表 | overview 只承载聚合指标；下方列表单独请求失败不能污染 overview 数据表现。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-09-04 | T | Dashboard 分页查询 | 缓解 | 统一使用参数化 SQL 与 `PageResult<T>` 模式，不拼接 keyword。 |
| T-09-05 | D | Dashboard 首页并行请求 | 缓解 | 模块级 loading/error 隔离，避免某单模块失败拖垮整页。 |
| T-09-06 | I | mock 清理后兜底逻辑 | 缓解 | 接口失败显式展示错误，不伪装为本地假数据成功。 |
</threat_model>

<verification>
## 自动化

- `cd backend && mvn -q -DskipTests compile`
- `cd frontend && npm run build`
- `rg "LIST-09|Phase 9" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`

## 手工建议

- Dashboard：overview 指标正常显示时，任一分页模块失败不导致整页失败
- 近期预警：切页后按标准分页切换，不再使用滚动增量加载
- 全局搜索与分页：四个目标页面都以后端请求结果为准
- mock 删除后：接口失败显示错误态，而不是悄悄回填本地假数据
</verification>

<success_criteria>
- Dashboard 完成 overview 与分页模块解耦
- 残留 mock 文件被移除
- Phase 9 的 roadmap / requirements / state 追溯补齐
</success_criteria>

<output>
执行完成后更新 `.planning/phases/09-mock/09-02-SUMMARY.md`
</output>

---

## PLANNING COMPLETE
