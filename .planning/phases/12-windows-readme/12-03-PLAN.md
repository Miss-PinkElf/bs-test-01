---
phase: 12-windows-readme
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scripts/verify-windows-portable.ps1
  - README.md
autonomous: true
requirements:
  - D-03
  - D-10
  - D-11
  - D-12
user_setup: []
must_haves:
  truths:
    - "portable 发布目录可以通过一个明确的自动化脚本完成结构与启动验证，而不是靠手工点点看。"
    - "根 README 会明确区分手动开发运行和 Windows portable 免安装运行两条路径。"
    - "README 会明确说明仓库不附带 portable 大文件，portable 包要由本地构建后自行压缩分发。"
  artifacts:
    - scripts/verify-windows-portable.ps1
    - README.md
  key_links:
    - "verify-windows-portable.ps1 -> start-portable.ps1 -> stop-portable.ps1 -> reset-portable-demo-db.ps1"
    - "README.md portable section -> .runtime-cache/windows inputs + release/windows-portable output + end-user commands"
---

<objective>
把 Phase 12 的交付收口为可复跑的 portable 验证脚本和面向仓库使用者的双运行 README，让执行结果有证据、分发方式有明确文案。

Purpose: packaging 和 runtime 落地后，还需要把“怎么证明它真能跑”和“开发者/答辩使用者分别怎么用”写清楚，否则 Phase 12 仍停留在只对实现者可读的状态。
Output: one portable verification script and a root README with two explicit run modes.
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-windows-readme/12-CONTEXT.md
@README.md
@package.json
@scripts/portable/start-portable.ps1
@scripts/portable/stop-portable.ps1
@scripts/portable/reset-portable-demo-db.ps1
@scripts/build-windows-portable.ps1
@scripts/verify-demo-baseline.ps1

<interfaces>
Expected end-user command names from Plan 02:

```powershell
.\start-portable.ps1
.\stop-portable.ps1
.\reset-portable-demo-db.ps1
```

Expected source-side build command from Plan 01:

```json
{
  "portable:build": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/build-windows-portable.ps1"
}
```

Planning implication:
- README 和 verifier 必须直接使用这些名字；不要再引入 `run-portable.ps1`、`portable-start.ps1` 等第二套命名。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 portable 验证脚本，自动证明 release 结构与启动链路</name>
  <read_first>
    - scripts/portable/start-portable.ps1
    - scripts/portable/stop-portable.ps1
    - scripts/portable/reset-portable-demo-db.ps1
    - scripts/build-windows-portable.ps1
    - scripts/verify-demo-baseline.ps1
    - .planning/phases/12-windows-readme/12-CONTEXT.md
  </read_first>
  <files>
    - scripts/verify-windows-portable.ps1
  </files>
  <action>
    1. 新建 `scripts/verify-windows-portable.ps1`，默认验证 `release/windows-portable/grain-platform-windows-portable/`：先断言 `runtime/jre/bin/java.exe`、`runtime/mysql/bin/mysqld.exe`、`runtime/mysql/bin/mysql.exe`、`app/grain-platform-backend.jar`、`start-portable.ps1`、`stop-portable.ps1`、`reset-portable-demo-db.ps1` 都存在，再调用 portable 主启动脚本。
    2. verifier 启动后要等待 `http://localhost:8081/login` 返回 HTML，且再至少命中一个后端接口（推荐 `/api/dashboard/overview` 或等价当前无鉴权接口）返回 `code=200`，证明 backend jar 与前端静态资源都在同一入口可用；随后执行 `reset-portable-demo-db.ps1`，并在 reset 后再次调用 `scripts/verify-demo-baseline.ps1`（经 bundled mysql）确认 demo 基线仍成立。
    3. verifier 无论成功还是失败都要调用 `stop-portable.ps1` 做清理，失败时返回非零退出码；不要让验证结束后留下孤儿 MySQL/Java 进程或模糊的“可能通过”状态。
    4. 脚本输出必须包含 release 目录、命中的 URL 和关键通过项，方便后续把它写进 `12-VERIFICATION.md`；但不要把数据库口令或整段 SQL 打进日志。
  </action>
  <acceptance_criteria>
    - `verify-windows-portable.ps1` 会同时验证 release 结构、portable 启动链路、API 可用性和 reset/baseline 语义。
    - verifier 自身带清理逻辑，失败后也不会遗留后台进程。
    - verifier 使用的脚本名与 Plan 02 产物完全一致，不引入第二套 portable 命名。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "grain-platform-windows-portable|java\\.exe|mysqld\\.exe|mysql\\.exe|start-portable\\.ps1|stop-portable\\.ps1|reset-portable-demo-db\\.ps1|http://localhost:8081/login|/api/dashboard/overview|verify-demo-baseline|exit 1|finally" scripts/verify-windows-portable.ps1</automated>
  </verify>
  <done>portable 发布目录已经有了可复跑的自动验证脚本，执行者后续可以直接据此生成 Phase 12 的验证证据。</done>
</task>

<task type="auto">
  <name>Task 2: 重写根 README 的双运行说明并明确 portable 分发边界</name>
  <read_first>
    - README.md
    - package.json
    - scripts/check-env.ps1
    - scripts/start-backend.ps1
    - scripts/start-frontend.ps1
    - scripts/start-all.ps1
    - scripts/build-windows-portable.ps1
    - scripts/portable/start-portable.ps1
    - scripts/portable/stop-portable.ps1
    - scripts/portable/reset-portable-demo-db.ps1
    - .planning/phases/12-windows-readme/12-CONTEXT.md
  </read_first>
  <files>
    - README.md
  </files>
  <action>
    1. 按 D-11 把根 README 的运行章节重写为两条明确路径，并保留当前项目简介/目录结构：`方式 1：手动开发运行`、`方式 2：Windows 免安装运行（portable）`。不要继续把它们混在同一段“启动方式”里。
    2. 在手动开发运行部分，明确列出需要安装 `node`、`npm`、`java`、`mvn`、`mysql`，并给出 `npm run check-env`、`npm run dev`、`npm run frontend`、`npm run backend`、`.\scripts\reset-demo-db.ps1` 的用途和适用场景；文案必须和当前脚本现实一致，不能再写“未检测到 mvn”这类一次性终端状态。
    3. 在 Windows portable 部分，明确列出源码侧生成步骤和分发边界：JRE 与 MySQL portable 需先放入 `.runtime-cache/windows/jre`、`.runtime-cache/windows/mysql`，运行 `npm run portable:build` 生成 `release/windows-portable/grain-platform-windows-portable/`；仓库本身不附带这些大文件，生成后的 release 目录由本地压缩后自行分发。
    4. 在 end-user portable 说明里明确 `.\start-portable.ps1`、`.\stop-portable.ps1`、`.\reset-portable-demo-db.ps1` 三个命令、默认访问地址 `http://localhost:8081`、portable MySQL 默认端口 `3307`、以及“默认保留现有数据，不会每次启动自动重置”的规则； README 不得暗示 portable 模式还需要安装 npm/mvn/mysql service。
  </action>
  <acceptance_criteria>
    - README 运行说明被拆成手动开发运行和 Windows portable 两节，且每节都有清晰的前置条件和命令。
    - README 明确写出 `.runtime-cache/windows/*` 输入、`release/windows-portable/grain-platform-windows-portable/` 输出，以及“仓库不附带 portable 大文件”的分发边界。
    - README 明确说明 portable 默认保留数据，reset 需单独执行 `reset-portable-demo-db.ps1`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "手动开发运行|Windows 免安装运行|portable|npm run check-env|npm run dev|npm run portable:build|\\.runtime-cache/windows/jre|\\.runtime-cache/windows/mysql|release/windows-portable/grain-platform-windows-portable|start-portable\\.ps1|stop-portable\\.ps1|reset-portable-demo-db\\.ps1|3307|仓库本身不附带 portable 大文件|默认保留现有数据" README.md</automated>
  </verify>
  <done>根 README 已经把开发者运行与答辩/演示 portable 运行分开说明，并把 portable 分发边界、命令和数据策略讲清楚。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier script -> portable release processes | 验证脚本需要实际启动和停止 bundled MySQL/JRE；清理失败会遗留进程并污染验证结果。 |
| README -> users generating/distributing portable package | README 是开发者和答辩使用者的主要入口，若命令名、路径或分发边界不准确，会直接误导使用。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | R | `scripts/verify-windows-portable.ps1` | mitigate | verifier 采用“存在性检查 -> 启动 -> URL/API 检查 -> reset/baseline -> stop”固定顺序，并在 finally/cleanup 路径始终调用停止脚本。 |
| T-12-12 | D | portable verification cleanup | mitigate | 失败时也要执行 `stop-portable.ps1` 并返回非零退出码，避免后台进程残留导致后续验证结果失真。 |
| T-12-13 | S | README portable instructions | mitigate | README 只引用真实已实现的脚本名、目录名和端口，不引入未落地命令或“仓库自带大文件”的错误暗示。 |
| T-12-14 | I | verifier logging | mitigate | 验证日志只输出目录、URL 和通过项，不输出数据库口令、整段 SQL 或敏感配置内容。 |
</threat_model>

<verification>
- `rg -n "grain-platform-windows-portable|java\\.exe|mysqld\\.exe|mysql\\.exe|start-portable\\.ps1|stop-portable\\.ps1|reset-portable-demo-db\\.ps1|http://localhost:8081/login|/api/dashboard/overview|verify-demo-baseline|exit 1|finally" scripts/verify-windows-portable.ps1`
- `rg -n "手动开发运行|Windows 免安装运行|portable|npm run check-env|npm run dev|npm run portable:build|\\.runtime-cache/windows/jre|\\.runtime-cache/windows/mysql|release/windows-portable/grain-platform-windows-portable|start-portable\\.ps1|stop-portable\\.ps1|reset-portable-demo-db\\.ps1|3307|仓库本身不附带 portable 大文件|默认保留现有数据" README.md`
</verification>

<success_criteria>
- portable 发布目录可以通过一个单独脚本自动验证结构、启动、reset 和清理链路。
- README 明确区分手动开发运行和 Windows portable 运行，不再混淆安装依赖与免安装使用者。
- 仓库与二进制分发的边界被清晰写出，符合 D-12。
</success_criteria>

<output>
After completion, create `.planning/phases/12-windows-readme/12-03-SUMMARY.md`
</output>
