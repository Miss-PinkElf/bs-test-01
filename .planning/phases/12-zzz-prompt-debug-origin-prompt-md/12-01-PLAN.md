---
phase: 12-zzz-prompt-debug-origin-prompt-md
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/DataView.vue
  - frontend/src/views/PredictionView.vue
  - frontend/src/views/BigScreenView.vue
  - frontend/src/styles.css
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-VERIFICATION.md
  - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-HUMAN-UAT.md
autonomous: true
requirements:
  - CHART-12-01
  - CHART-12-02
  - CHART-12-03
  - CHART-12-04
user_setup: []

must_haves:
  truths:
    - `frontend/src/views/DataView.vue` 的粮温汇总趋势图继续留在现有页面结构内，且支持用户主动选择时间范围，不扩到 `frontend-next`
    - `frontend/src/views/PredictionView.vue` 的实际值 / 预测值双线图新增图表级时间范围能力，时间范围只影响图表显示，不改变任务摘要和历史记录语义
    - `frontend/src/views/BigScreenView.vue` 的粮温趋势主图继续复用页面现有 `screenFilters.timeRange` 共享查询，不新增第二套图表时间范围状态
    - 三张图的主解法是“时间范围优先、图表交互兜底”，不能只靠标签旋转硬撑全部历史
    - `DataView.vue`、`PredictionView.vue` 与 `BigScreenView.vue` 的目标图都落地 ECharts x 轴密度收口，如 `axisLabel` 抽样/格式化、适度旋转、`dataZoom` 等，使长时间轴仍可查看
    - Phase 12 在 `.planning/REQUIREMENTS.md`、`.planning/ROADMAP.md`、`.planning/STATE.md` 中不再保持 `TBD` / 缺失状态，并补齐 `12-VERIFICATION.md` 与 `12-HUMAN-UAT.md`
  artifacts:
    - frontend/src/views/DataView.vue
    - frontend/src/views/PredictionView.vue
    - frontend/src/views/BigScreenView.vue
    - frontend/src/styles.css
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
  key_links:
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
    - zzz-prompt-debug/origin/优化图表/prompt.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
    - .planning/phases/06-overflow-x/06-CONTEXT.md
---

<objective>
落实 Phase 12：保留并增强 `DataView.vue` 的粮温汇总趋势图、`PredictionView.vue` 的实际值 / 预测值双线图，以及 `BigScreenView.vue` 的粮温趋势主图，让三张图都建立在可控时间范围上，并用更稳的 x 轴策略解决“时间轴过长、展示不全”的问题。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@frontend/src/views/DataView.vue
@frontend/src/views/PredictionView.vue
@frontend/src/views/BigScreenView.vue
@frontend/src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: DataView / BigScreen 粮温趋势图的时间范围与 x 轴收口</name>
  <read_first>
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
    - .planning/phases/07-zzz-prompt-debug-origin-prompt-md/07-CONTEXT.md
    - .planning/phases/08-zzz-prompt-debug-origin/08-CONTEXT.md
    - frontend/src/views/DataView.vue
    - frontend/src/views/BigScreenView.vue
    - frontend/src/styles.css
  </read_first>
  <files>
    - frontend/src/views/DataView.vue
    - frontend/src/views/BigScreenView.vue
  </files>
  <action>
    1. 保留 `DataView.vue` 当前粮温主线的页面结构、共享查询区和 `grainSummaryRange` 查询口径，不把粮温汇总趋势图拆到新组件或新页面。
    2. 为粮温汇总趋势图补“默认时间窗口”能力：页面初次进入粮温主线时，如果 `grainSummaryRange` 为空，则自动设置为最近 30 天，格式继续使用 `YYYY-MM-DD HH:mm:ss`；用户手动修改后仍以用户选择为准。
    3. 粮温汇总趋势图继续走当前共享查询链路：仓库 + `grainSummaryRange` 共同驱动 `fetchGrainTempSummaries`；不要把图表改成和表格分离的第二套时间范围状态。
    4. 在 ECharts 配置中为粮温汇总趋势图增加长时间轴兜底：
       - `grid.bottom` 留出标签和缩放控件空间
       - `xAxis.axisLabel` 使用时间格式压缩与抽样策略，避免每个点都完整显示到秒
       - `xAxis.axisLabel.rotate` 在点位较密时适度旋转
       - 增加 `dataZoom`（至少包含一个 `inside` 或底部 `slider` 方案），让长窗口仍可滑动查看
    5. 在 `BigScreenView.vue` 的粮温趋势主图里，继续复用页面现有 `screenFilters.timeRange` 共享查询，不新增图表级独立时间范围状态；图表继续保留“粮温趋势 + 最高温”两条线语义。
    6. 为 `BigScreenView.vue` 的粮温趋势主图同步增加长时间轴兜底：`grid.bottom`、`axisLabel` 抽样/格式化、适度 `rotate`、`dataZoom`；保证截图里这种大屏折线图在长窗口下也不会把 x 轴挤爆。
  </action>
  <acceptance_criteria>
    - `rg "grainSummaryRange" frontend/src/views/DataView.vue` 命中默认时间窗口初始化与原有共享查询状态。
    - `rg "30" frontend/src/views/DataView.vue` 命中最近 30 天默认窗口的具体实现或等价常量。
    - `rg "dataZoom|axisLabel|rotate|grid:" frontend/src/views/DataView.vue` 命中粮温汇总趋势图的 x 轴密度收口配置。
    - `rg "screenFilters.timeRange|dataZoom|axisLabel|rotate|粮温趋势|最高温" frontend/src/views/BigScreenView.vue` 命中大屏粮温趋势主图继续复用共享时间范围且补了 x 轴密度收口配置。
    - `rg "最高温" frontend/src/views/DataView.vue` 仍命中固定参考线，证明图表主语义未被删除。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>DataView 与 BigScreen 的粮温趋势图都能在既有查询语义内稳定承载长时间轴，且可继续缩放或滑动查看。</done>
</task>

<task type="auto">
  <name>Task 2: 预测页双线图增加图表级时间范围筛选</name>
  <read_first>
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
    - frontend/src/views/PredictionView.vue
    - frontend/src/api/grain.js
  </read_first>
  <files>
    - frontend/src/views/PredictionView.vue
    - frontend/src/styles.css
  </files>
  <action>
    1. 在 `PredictionView.vue` 的“实际值 / 预测值双线图”卡片中新增图表级时间范围控件，状态只服务当前图表显示，不回流修改任务摘要、任务本身的预测区间或历史任务列表查询。
    2. 图表级时间范围默认采用较短窗口，优先保证当前任务首屏可读：优先使用当前任务结果时间线里的最近 7 天可视窗口；若当前任务时间点不足 7 天，则退化为展示全部 `resultList`。
    3. 对当前任务 `prediction.value.resultList` 增加前端过滤视图，例如基于 `filteredPredictionResultList` 或等价计算态，图表的 `xAxis.data`、`实际值`、`预测值` 三处都只消费过滤后的结果，不直接继续使用全量 `resultList`。
    4. 如果用户清空时间范围，则图表恢复显示当前任务完整时间线；清空行为只影响图表，不影响任务摘要中的 `trainStartTime / trainEndTime / forecastStartTime / forecastEndTime` 展示。
    5. 在图表 ECharts 配置中同步增加长时间轴兜底：`axisLabel` 压缩、适度 `rotate`、`dataZoom`；双线图继续保持“实际值 / 预测值”两条线及当前配色，不删除现有面积填充和滚动锚点行为。
  </action>
  <acceptance_criteria>
    - `rg "resultList" frontend/src/views/PredictionView.vue` 仍命中原始任务数据，但图表渲染逻辑同时命中过滤后的图表数据源，而不是全量直出。
    - `rg "7" frontend/src/views/PredictionView.vue` 命中默认最近 7 天可视窗口或等价常量。
    - `rg "dataZoom|axisLabel|rotate" frontend/src/views/PredictionView.vue` 命中双线图的 x 轴密度收口配置。
    - `rg "forecastStartTime|forecastEndTime|trainStartTime|trainEndTime" frontend/src/views/PredictionView.vue` 仍命中任务摘要字段，且这些字段不依赖新的图表时间范围状态。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>预测页双线图支持图表级时间范围筛选，且不改变当前任务其它区域的业务语义。</done>
</task>

<task type="auto">
  <name>Task 3: 统一补充 Phase 12 需求、路线图与验收文档</name>
  <read_first>
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-VERIFICATION.md
    - .planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-HUMAN-UAT.md
  </files>
  <action>
    1. 在 `.planning/REQUIREMENTS.md` 中为 Phase 12 明确 requirement IDs：`CHART-12-01`、`CHART-12-02`、`CHART-12-03`、`CHART-12-04`，分别覆盖：
       - 粮温汇总趋势图支持时间范围与可读的长 x 轴
       - 预测页实际值 / 预测值双线图支持图表级时间范围且不改任务语义
       - 展示大屏的粮温趋势主图继续复用共享时间范围并具备可读的长 x 轴
       - 三张图采用“时间范围优先、图表交互兜底”的收口方式
    2. 更新 `.planning/ROADMAP.md` 中 Phase 12 的 Goal / Requirements / Success Criteria / Plans，不再保留 `[To be planned]` 与 `TBD`，并明确 `12-01-PLAN.md` 已创建。
    3. 在 `.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-VERIFICATION.md` 写入至少两类验证：
       - 自动化：`cd frontend && npm run build`
       - 手工：DataView 粮温汇总趋势图默认 30 天窗口、PredictionView 双线图默认 7 天图表窗口、BigScreen 粮温趋势主图长 x 轴可滑动查看、三张图时间范围行为符合既有语义
    4. 在 `.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-HUMAN-UAT.md` 预写本阶段人工验收清单，覆盖用户最关心的“图还在、时间范围能选、x 轴不再展示不全”。
    5. `.planning/STATE.md` 至少追加一条 Phase 12 planning 记录；若 `gsd-tools state` 继续因当前 STATE 结构不兼容而失败，需要在记录中明确说明本仓库继续采用人工同步方式。
  </action>
  <acceptance_criteria>
    - `rg "CHART-12-01|CHART-12-02|CHART-12-03|CHART-12-04" .planning/REQUIREMENTS.md` 命中新 requirement IDs。
    - `rg "Phase 12|12-01-PLAN.md|CHART-12" .planning/ROADMAP.md` 命中已去除 TBD 的 Phase 12 条目。
    - `.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-VERIFICATION.md` 存在，并包含 `npm run build`。
    - `.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-HUMAN-UAT.md` 存在，并包含 DataView / PredictionView / BigScreen 的时间范围与 x 轴验收点。
  </acceptance_criteria>
  <verify>
    <automated>rg "CHART-12|12-01-PLAN.md" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 12 的需求、路线图与验收文档具备完整追溯链，不再停留在占位状态。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 管理台浏览器 → 图表时间范围控件 | 新增时间范围来自前端交互，只能影响前端查询参数或图表过滤视图，不得绕开现有后端查询边界。 |
| `PredictionView` 图表过滤态 → 当前任务业务语义 | 图表时间范围是查看窗口，不是任务定义本身，必须防止把图表筛选误当成任务区间变更。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-12-01 | T | PredictionView 图表时间范围 | 缓解 | 新时间范围状态只驱动图表计算态，不回写 `prediction.value` 的任务元数据字段。 |
| T-12-02 | D | 长时间轴图表渲染 | 缓解 | 通过默认时间窗口与 `dataZoom` 降低首屏点位密度，避免一次性渲染过长时间轴导致体验恶化。 |
| T-12-03 | I | 文档与路线图追溯 | 缓解 | 在执行阶段同步 `REQUIREMENTS/ROADMAP/STATE/VERIFICATION/HUMAN-UAT`，避免 Phase 12 长期停留在 `TBD`。 |
</threat_model>

<verification>
## 自动化

- `cd frontend && npm run build`
- `rg "screenFilters.timeRange|dataZoom|axisLabel|rotate|粮温趋势|最高温" frontend/src/views/BigScreenView.vue`
- `rg "CHART-12|12-01-PLAN.md" .planning/REQUIREMENTS.md .planning/ROADMAP.md`

## 手工建议

- 进入 `DataView` 粮温主线，确认粮温汇总趋势图默认落在最近 30 天，且用户仍可手动修改时间范围
- 在粮温汇总趋势图里选择更长时间范围，确认 x 轴不会再次完整挤爆，且可通过缩放或滑动查看
- 进入 `PredictionView`，确认双线图默认是较短图表窗口，且任务摘要仍展示完整任务区间
- 修改 `PredictionView` 图表时间范围后，确认只有图变化，任务摘要和历史任务列表不被一起重置
- 清空 `PredictionView` 图表时间范围后，确认双线图恢复完整时间线
- 进入 `/screen`，确认粮温趋势主图在当前共享时间范围下也能承载更长时间轴，且可通过缩放或滑动查看，不新增额外查询状态
</verification>

<success_criteria>
- 粮温汇总趋势图与预测页双线图都支持用户选择时间范围，大屏粮温趋势主图继续复用共享时间范围。
- 三张图都通过时间范围优先与图表交互兜底解决了长 x 轴展示不全问题。
- `PredictionView` 的图表时间范围没有污染任务摘要和历史记录的业务语义。
- Phase 12 的文档链路不再停留在 `TBD` / 未收口状态。
</success_criteria>

<output>
执行完成后更新 `.planning/phases/12-zzz-prompt-debug-origin-prompt-md/12-01-SUMMARY.md`
</output>

---

## PLANNING COMPLETE
