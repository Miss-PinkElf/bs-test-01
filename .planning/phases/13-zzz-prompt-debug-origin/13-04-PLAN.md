---
phase: 13-zzz-prompt-debug-origin
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md
  - .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md
autonomous: true
requirements:
  - AUTHZ-13-04
user_setup: []

must_haves:
  truths:
    - Phase 13 不能长期停留在 `ROADMAP.md` 的 `Requirements: TBD` 状态，必须把角色菜单、角色范围、后端 403 和只读语义映射到正式 requirement IDs。
    - 执行完成后必须有明确的验证链，能覆盖 `admin / manager / viewer` 三类账号的登录、菜单、路由、读写权限和越权场景。
    - 当前仓库的 `STATE.md` 结构可能与 `gsd-tools state` 的自动写入不完全兼容，本计划需要把这一限制明确记录到文档里，而不是静默跳过。
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md
  key_links:
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-01-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-02-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-03-PLAN.md
---

<objective>
在 Phase 13 的代码计划之外，补齐本阶段的需求追溯、路线图、状态记录与验证文档，让“角色菜单与权限区分”在 GSD 文档链路里真正可追溯、可验收、可交接，而不是只停留在 `CONTEXT.md` 和代码实现层。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/router/index.js
@frontend/src/layout/ConsoleLayout.vue
@frontend/src/views/DataView.vue
@frontend/src/views/PredictionView.vue
@backend/src/main/java/com/grain/platform/common/GlobalExceptionHandler.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补齐 Phase 13 requirement IDs 与 roadmap 条目</name>
  <read_first>
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-01-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-02-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-03-PLAN.md
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </files>
  <action>
    1. 在 `.planning/REQUIREMENTS.md` 为 Phase 13 新增并固定以下 requirement IDs：
       - `AUTHZ-13-01`：后台菜单、路由守卫、默认落点按角色区分
       - `AUTHZ-13-02`：`WAREHOUSE_MANAGER` 单仓能力与 `VIEWER` 全局只读语义前后端一致
       - `AUTHZ-13-03`：后端越权明确返回 `403`，并对管理接口、数据接口、预测接口做角色/仓库范围门禁
       - `AUTHZ-13-04`：Phase 13 的 `ROADMAP / STATE / VERIFICATION / HUMAN-UAT / SUMMARY` 不再停留在 `TBD` 或缺失状态
    2. 更新 `.planning/ROADMAP.md` 中 Phase 13 的 `Requirements`、`Success criteria`、`Plans`，把当前 `TBD` 收口成实际 requirement IDs 和已生成的 `13-01-PLAN.md` ~ `13-04-PLAN.md`。
    3. 同步核对 4 个 plan frontmatter 的 `requirements:` 字段和 `.planning/REQUIREMENTS.md` 里的命名保持一致，不要出现第二套 `ROLE-13-*`、`RBAC-13-*` 或其它别名。
  </action>
  <acceptance_criteria>
    - `rg "AUTHZ-13-01|AUTHZ-13-02|AUTHZ-13-03|AUTHZ-13-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/13-zzz-prompt-debug-origin/13-0*-PLAN.md` 命中统一后的 requirement IDs。
    - `rg "13-01-PLAN.md|13-02-PLAN.md|13-03-PLAN.md|13-04-PLAN.md" .planning/ROADMAP.md` 命中 Phase 13 计划列表已不再是 `TBD`。
  </acceptance_criteria>
  <verify>
    <automated>rg "AUTHZ-13-01|AUTHZ-13-02|AUTHZ-13-03|AUTHZ-13-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/13-zzz-prompt-debug-origin/13-0*-PLAN.md</automated>
  </verify>
  <done>Phase 13 已具备正式 requirement IDs，路线图条目不再停留在 `TBD`。</done>
</task>

<task type="auto">
  <name>Task 2: 预写 Phase 13 验证与人工验收清单</name>
  <read_first>
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - frontend/src/router/index.js
    - frontend/src/layout/ConsoleLayout.vue
    - frontend/src/views/DataView.vue
    - frontend/src/views/PredictionView.vue
    - backend/src/main/java/com/grain/platform/common/GlobalExceptionHandler.java
  </read_first>
  <files>
    - .planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md
  </files>
  <action>
    1. 新建 `.planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md`，至少包含：
       - 自动化验证：`cd frontend && npm run build`、`cd backend && mvn -q -DskipTests compile`
       - 代码证据：菜单 `allowedRoles`、`defaultRoute`、`X-Demo-Username`、`ForbiddenException`、`403`、`resolveWarehouseScope`
       - 后端语义验证：越权返回 `403` 而非 `400`
    2. 新建 `.planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md`，按账号维度写清楚人工验收步骤：
       - `admin / 123456`
       - `manager_a01 / 123456`
       - `viewer_demo / 123456`
       每个账号都至少覆盖登录后默认落点、可见菜单、可访问页面、写操作按钮是否存在、直接输无权限 URL 的行为、以及典型越权操作的预期结果。
    3. `13-HUMAN-UAT.md` 必须显式覆盖两个关键跨层场景：
       - `manager_a01` 不能查看或修改其它仓库数据
       - `viewer_demo` 能看但不能新增、编辑、删除、导入或执行预测
  </action>
  <acceptance_criteria>
    - `.planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md` 存在，并包含 `npm run build`、`mvn -q -DskipTests compile`、`ForbiddenException`、`resolveWarehouseScope`。
    - `.planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md` 存在，并同时命中 `admin`、`manager_a01`、`viewer_demo` 三个账号。
    - `rg "默认落点|可见菜单|403|manager_a01|viewer_demo" .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md` 命中关键人工验收项。
  </acceptance_criteria>
  <verify>
    <automated>rg "npm run build|mvn -q -DskipTests compile|ForbiddenException|resolveWarehouseScope|manager_a01|viewer_demo" .planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md</automated>
  </verify>
  <done>Phase 13 已具备可执行的自动化验证清单和按角色分组的人工验收脚本。</done>
</task>

<task type="auto">
  <name>Task 3: STATE.md 记录 planning 落盘与兼容性限制</name>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-01-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-02-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-03-PLAN.md
    - .planning/phases/13-zzz-prompt-debug-origin/13-04-PLAN.md
  </read_first>
  <files>
    - .planning/STATE.md
  </files>
  <action>
    1. 在 `.planning/STATE.md` 的 Session Notes 或等价阶段记录位置追加一条 Phase 13 planning 落盘记录，至少说明：
       - 已完成 `13-CONTEXT.md`
       - 已生成 `13-01-PLAN.md` ~ `13-04-PLAN.md`
       - 当前阶段目标为“角色菜单与权限区分（前端菜单 + 后端权限）”
    2. 如果执行时 `gsd-tools state planned-phase` 或其它自动状态命令仍因当前 `STATE.md` 结构不兼容失败，必须把这一事实明确写入本条记录，说明“本仓库继续采用人工同步方式”，而不是静默省略。
    3. 不要大改 `STATE.md` 结构，只在现有人工维护格式内补充这次 planning 事实与限制说明。
  </action>
  <acceptance_criteria>
    - `rg "Phase 13|13-01-PLAN.md|13-02-PLAN.md|13-03-PLAN.md|13-04-PLAN.md" .planning/STATE.md` 命中新 planning 记录。
    - `rg "人工同步|不兼容|gsd-tools state" .planning/STATE.md` 命中对当前 STATE 自动写入兼容性限制的明确说明或等价表述。
  </acceptance_criteria>
  <verify>
    <automated>rg "Phase 13|13-01-PLAN.md|人工同步|不兼容|gsd-tools state" .planning/STATE.md</automated>
  </verify>
  <done>Phase 13 的 planning 状态与当前 STATE 兼容性限制都已被显式记录。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 代码实现 → GSD 文档链路 | 若不补 REQUIREMENTS / ROADMAP / VERIFICATION / HUMAN-UAT，Phase 13 会再次出现“代码做了但阶段文档仍是 TBD”的断层。 |
| `STATE.md` 手工维护结构 → gsd-tools 自动状态命令 | 当前仓库 `STATE.md` 已存在结构兼容性风险，必须明确记录限制，避免后续误判“状态已自动同步”。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | R | Phase 13 文档追溯 | mitigate | 把 Phase 13 requirement IDs、plans、verification、UAT 全部落盘，避免阶段事实无法追踪。 |
| T-13-11 | I | STATE 自动写入状态 | mitigate | 在 STATE 里显式记录 gsd-tools 兼容性限制和人工同步方案，不让后续协作者误以为状态已自动收口。 |
</threat_model>

<verification>
- `rg "AUTHZ-13-01|AUTHZ-13-02|AUTHZ-13-03|AUTHZ-13-04" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/13-zzz-prompt-debug-origin/13-0*-PLAN.md`
- `rg "npm run build|mvn -q -DskipTests compile|manager_a01|viewer_demo|403" .planning/phases/13-zzz-prompt-debug-origin/13-VERIFICATION.md .planning/phases/13-zzz-prompt-debug-origin/13-HUMAN-UAT.md`
- `rg "Phase 13|13-01-PLAN.md|人工同步|不兼容|gsd-tools state" .planning/STATE.md`
</verification>

<success_criteria>
- Phase 13 的 requirement IDs、路线图条目和 plan 引用已经正式化，不再是 `TBD`。
- 本阶段已有可执行的自动化验证与人工 UAT 清单。
- STATE 中已经明确记录 Phase 13 planning 事实和自动状态写入兼容性限制。
</success_criteria>

<output>
After completion, create `.planning/phases/13-zzz-prompt-debug-origin/13-04-SUMMARY.md`
</output>
