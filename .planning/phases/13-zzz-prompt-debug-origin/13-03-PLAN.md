---
phase: 13-zzz-prompt-debug-origin
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/src/main/java/com/grain/platform/controller/UserController.java
  - backend/src/main/java/com/grain/platform/controller/WarehouseController.java
  - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
  - backend/src/main/java/com/grain/platform/controller/SensorDataController.java
  - backend/src/main/java/com/grain/platform/controller/PredictionController.java
  - backend/src/main/java/com/grain/platform/controller/DashboardController.java
  - backend/src/main/java/com/grain/platform/service/WarehouseService.java
  - backend/src/main/java/com/grain/platform/service/GrainTempService.java
  - backend/src/main/java/com/grain/platform/service/SensorDataService.java
  - backend/src/main/java/com/grain/platform/service/PredictionService.java
  - backend/src/main/java/com/grain/platform/service/DashboardService.java
  - backend/src/main/java/com/grain/platform/service/GrainTempImportService.java
autonomous: true
requirements:
  - AUTHZ-13-02
  - AUTHZ-13-03
user_setup: []

must_haves:
  truths:
    - 后端必须真正做角色与仓库范围校验，不能只靠前端隐藏菜单或按钮。
    - `ADMIN` 具备全局读写；`WAREHOUSE_MANAGER` 仅能读写所属仓库；`VIEWER` 仅能读，且任何新增、编辑、删除、导入、执行预测都应收到 `403`。
    - 用户管理和仓库管理属于平台配置能力，必须限制为 `ADMIN`。
    - 环境数据、粮温、预测、仪表盘的读取接口需要对 `WAREHOUSE_MANAGER` 自动收口到自己的 `warehouseId`，而不是允许带任意 `warehouseId` 参数穿透到底层查询。
    - 写入链路的 `createdBy` / `requestedBy` 不能继续写死 `1L`，必须使用当前操作用户的真实 `userId`。
  artifacts:
    - backend/src/main/java/com/grain/platform/controller/UserController.java
    - backend/src/main/java/com/grain/platform/controller/WarehouseController.java
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/controller/SensorDataController.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/controller/DashboardController.java
  key_links:
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - backend/src/main/java/com/grain/platform/security/AccessControlService.java
    - backend/src/main/resources/db/schema.sql
    - backend/src/main/java/com/grain/platform/service/AuthService.java
---

<objective>
完成 Phase 13 的后端权限收口：把平台级管理接口限制为 `ADMIN`，把环境数据 / 粮温 / 预测 / 仪表盘按角色自动收口到正确的读写范围，并把所有越权访问明确返回 `403`。同时把新增、导入、预测任务写入人从写死的 `1L` 改为当前真实用户，保证角色语义和数据审计一致。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
@backend/src/main/java/com/grain/platform/security/AccessControlService.java
@backend/src/main/java/com/grain/platform/controller/UserController.java
@backend/src/main/java/com/grain/platform/controller/WarehouseController.java
@backend/src/main/java/com/grain/platform/controller/GrainTempController.java
@backend/src/main/java/com/grain/platform/controller/SensorDataController.java
@backend/src/main/java/com/grain/platform/controller/PredictionController.java
@backend/src/main/java/com/grain/platform/controller/DashboardController.java
@backend/src/main/java/com/grain/platform/service/WarehouseService.java
@backend/src/main/java/com/grain/platform/service/GrainTempService.java
@backend/src/main/java/com/grain/platform/service/SensorDataService.java
@backend/src/main/java/com/grain/platform/service/PredictionService.java
@backend/src/main/java/com/grain/platform/service/DashboardService.java
@backend/src/main/java/com/grain/platform/service/GrainTempImportService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: 平台管理接口改为仅管理员可用，仓库列表接口按角色收口</name>
  <read_first>
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - backend/src/main/java/com/grain/platform/security/AccessControlService.java
    - backend/src/main/java/com/grain/platform/controller/UserController.java
    - backend/src/main/java/com/grain/platform/controller/WarehouseController.java
    - backend/src/main/java/com/grain/platform/service/WarehouseService.java
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/controller/UserController.java
    - backend/src/main/java/com/grain/platform/controller/WarehouseController.java
    - backend/src/main/java/com/grain/platform/service/WarehouseService.java
  </files>
  <action>
    1. `UserController` 全部接口统一接收 `@RequestHeader(value = "X-Demo-Username", required = false) String username`，并在进入业务前调用 `AccessControlService.requireAdmin(username)`；非管理员访问用户管理相关接口一律返回 `403`。
    2. `WarehouseController` 区分两类接口：
       - `/api/warehouses/page`、`/api/warehouses/stats`、`POST /api/warehouses`、`PUT /api/warehouses/{id}`、`DELETE /api/warehouses/{id}` → 仅 `ADMIN`
       - `/api/warehouses`、`/api/warehouses/options` → 所有已登录用户可访问，但 `WAREHOUSE_MANAGER` 只能拿到自己的 `warehouseId` 对应仓库，`ADMIN` 和 `VIEWER` 仍可拿到全量
    3. `WarehouseService` 补必要的按单仓返回能力，让控制器在 manager 场景下能稳定返回“仅自己仓库”的列表或选项，而不是继续全量返回后交给前端自己过滤。
    4. 不要把 `VIEWER` 错拦到 `fetchWarehouses()` 这种只读选择数据上；查看者仍需要全量仓库选项来做全局查看。
    5. 所有平台管理接口的 `403` 文案明确写成角色语义，例如“仅管理员可管理用户”“仅管理员可维护仓库”，不要继续复用通用参数错误提示。
  </action>
  <acceptance_criteria>
    - `rg "X-Demo-Username|requireAdmin" backend/src/main/java/com/grain/platform/controller/UserController.java backend/src/main/java/com/grain/platform/controller/WarehouseController.java` 命中管理接口管理员门禁。
    - `rg "/page|/stats|@PostMapping|@PutMapping|@DeleteMapping" backend/src/main/java/com/grain/platform/controller/WarehouseController.java` 仍命中仓库管理接口，且相关方法附近新增了管理员校验。
    - `rg "selectById|listVisible|optionsVisible|only own warehouse|自己的仓库" backend/src/main/java/com/grain/platform/service/WarehouseService.java` 命中面向 manager 单仓可见范围的收口逻辑或等价实现。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>用户管理和仓库管理已经按角色拆分为“管理员专属 + 只读仓库列表”两种后端语义。</done>
</task>

<task type="auto">
  <name>Task 2: 粮温、环境数据与预测接口按角色和仓库范围收口</name>
  <read_first>
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - backend/src/main/java/com/grain/platform/security/AccessControlService.java
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/controller/SensorDataController.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/service/SensorDataService.java
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/service/GrainTempImportService.java
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/controller/GrainTempController.java
    - backend/src/main/java/com/grain/platform/controller/SensorDataController.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - backend/src/main/java/com/grain/platform/service/SensorDataService.java
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/service/GrainTempImportService.java
  </files>
  <action>
    1. `GrainTempController`、`SensorDataController`、`PredictionController` 的所有接口都接收 `X-Demo-Username` 并基于 `AccessControlService.requireCurrentUser(...)` 取得当前用户。
    2. 对读取接口统一按角色收口有效仓库范围：
       - `WAREHOUSE_MANAGER`：不管前端传什么 `warehouseId`，都强制使用 `CurrentUserContext.warehouseId`
       - `ADMIN`：允许沿用请求参数或查看全量
       - `VIEWER`：允许查看全量或按请求 `warehouseId` 缩小范围
       需要覆盖的读取接口至少包括：
       - `GET /api/grain-temp/records/filter-options`
       - `GET /api/grain-temp/records`
       - `GET /api/grain-temp/summaries`
       - `GET /api/grain-temp/summaries/page`
       - `GET /api/sensor-data`
       - `GET /api/sensor-data/trend`
       - `GET /api/predictions/tasks`
       - `GET /api/predictions/tasks/page`
       - `GET /api/predictions/tasks/{taskId}`
    3. 对写接口统一按角色收口：
       - `VIEWER`：`POST/PUT/DELETE`、导入、执行预测全部抛 `ForbiddenException`
       - `WAREHOUSE_MANAGER`：只能对自己仓库的数据执行写操作，包含粮温新增/编辑/删除/导入、环境数据新增/编辑/删除/导入、预测执行、预测任务删除
       - `ADMIN`：保留全局写权限
    4. 写接口不能只校验请求体里的 `warehouseId`；像 `DELETE /api/predictions/tasks/{taskId}`、`DELETE /api/grain-temp/records/{id}`、`DELETE /api/sensor-data/{id}` 这种 path-id 操作，必须先取出现有记录或任务的真实 `warehouseId` 再校验是否允许删除。
    5. 把 `GrainTempService`、`SensorDataService`、`GrainTempImportService`、`PredictionService` 中当前写死的 `createdBy = 1L`、`requestedBy = 1L` 改为接收当前操作者 `userId` 并落库。控制器在完成权限校验后，把真实 `CurrentUserContext.userId` 传入 service。
    6. 预测读取链路里，`WAREHOUSE_MANAGER` 访问任务详情、列表、删除时必须按任务所属 `warehouseId` 限制，不允许通过 taskId 越仓查看或删除其他仓库任务。
  </action>
  <acceptance_criteria>
    - `rg "X-Demo-Username|requireCurrentUser|resolveWarehouseScope" backend/src/main/java/com/grain/platform/controller/GrainTempController.java backend/src/main/java/com/grain/platform/controller/SensorDataController.java backend/src/main/java/com/grain/platform/controller/PredictionController.java` 命中当前用户与仓库范围解析逻辑。
    - `rg "ForbiddenException|assertWarehouseWriteAccess" backend/src/main/java/com/grain/platform/controller/GrainTempController.java backend/src/main/java/com/grain/platform/controller/SensorDataController.java backend/src/main/java/com/grain/platform/controller/PredictionController.java backend/src/main/java/com/grain/platform/service` 命中写操作越权校验。
    - `rg "setCreatedBy\\(|setRequestedBy\\(" backend/src/main/java/com/grain/platform/service/GrainTempService.java backend/src/main/java/com/grain/platform/service/SensorDataService.java backend/src/main/java/com/grain/platform/service/GrainTempImportService.java backend/src/main/java/com/grain/platform/service/PredictionService.java` 仍命中落库逻辑，且不再写死 `1L`。
    - `rg "1L" backend/src/main/java/com/grain/platform/service/GrainTempService.java backend/src/main/java/com/grain/platform/service/SensorDataService.java backend/src/main/java/com/grain/platform/service/GrainTempImportService.java backend/src/main/java/com/grain/platform/service/PredictionService.java` 不再命中作为操作者 id 的硬编码。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>环境数据、粮温与预测接口已经按角色和仓库范围真正收口，并把真实操作者写入审计字段。</done>
</task>

<task type="auto">
  <name>Task 3: 仪表盘后端读取范围与 403 语义对齐</name>
  <read_first>
    - .planning/phases/13-zzz-prompt-debug-origin/13-CONTEXT.md
    - backend/src/main/java/com/grain/platform/security/AccessControlService.java
    - backend/src/main/java/com/grain/platform/controller/DashboardController.java
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
    - backend/src/main/java/com/grain/platform/mapper/DashboardMapper.java
    - frontend/src/views/DashboardView.vue
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/controller/DashboardController.java
    - backend/src/main/java/com/grain/platform/service/DashboardService.java
  </files>
  <action>
    1. `DashboardController` 的 `overview`、`alerts`、`warehouse-health`、`grain-summaries`、`screen` 全部接入当前用户上下文；`/screen` 仍保持公开，不做后台角色限制，其他 `/api/dashboard/*` 后台接口都按已登录用户身份收口。
    2. 对后台仪表盘接口的读取范围统一按角色处理：
       - `WAREHOUSE_MANAGER`：顶部概览、近期预警、仓库运行健康度、最新粮温汇总都只看到自己仓库的数据
       - `ADMIN` / `VIEWER`：默认全局
    3. `DashboardService` 新增或改造支持“可选仓库范围”的方法，不要继续让 manager 走全局 `countWarehouses()` / `countLatestRealAlertCount()` 这种无范围聚合。
    4. 不修改 `/screen` 当前公开展示定位，但若未来它被后台调用复用，也要允许传入明确 `warehouseId` 继续工作；本任务不把 `/screen` 纳入后台角色门禁。
    5. 对后台仪表盘接口使用的 403 文案保持清晰，例如“未登录无法访问后台仪表盘”“当前角色无权访问该后台资源”，不要继续返回含糊的通用失败语义。
  </action>
  <acceptance_criteria>
    - `rg "X-Demo-Username|requireCurrentUser" backend/src/main/java/com/grain/platform/controller/DashboardController.java` 命中后台仪表盘当前用户解析逻辑。
    - `rg "warehouseId|scope|manager" backend/src/main/java/com/grain/platform/service/DashboardService.java` 命中按角色收口后台仪表盘读取范围的实现或等价注释。
    - `rg "@GetMapping\\(\"/screen\"\\)" backend/src/main/java/com/grain/platform/controller/DashboardController.java` 仍命中公开大屏入口，证明未被误改为后台专用。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>后台仪表盘接口已与角色/仓库范围语义对齐，manager 不再看到全局后台数据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `AccessControlService` → 后台控制器 | 后台所有角色门禁都依赖这层上下文与断言，任何控制器绕开它都会造成权限旁路。 |
| path-id 删除/详情接口 → 实际仓库归属 | 对 `taskId`、`recordId` 这类路径参数，必须先查出真实仓库归属再判断权限，不能只看前端传参。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | E | 用户/仓库管理接口 | mitigate | 所有平台管理接口统一 `requireAdmin`，非管理员一律 `403`。 |
| T-13-08 | E | manager 越仓读写 | mitigate | 对读接口使用 `resolveWarehouseScope`，对写接口和 path-id 删除使用 `assertWarehouseWriteAccess` + 实际归属校验。 |
| T-13-09 | R | 数据操作者审计 | mitigate | 把 `createdBy` / `requestedBy` 从硬编码 `1L` 改为真实当前用户 id。 |
</threat_model>

<verification>
- `cd backend && mvn -q -DskipTests compile`
- `rg "requireAdmin|requireCurrentUser|resolveWarehouseScope|assertWarehouseWriteAccess" backend/src/main/java`
- `rg "setCreatedBy\\(|setRequestedBy\\(" backend/src/main/java/com/grain/platform/service`
</verification>

<success_criteria>
- 后端真正限制了管理员、仓库管理员、查看者三类角色的接口权限。
- 仓库管理员无法跨仓查看或修改后台业务数据。
- 写入链路的操作者 id 已从硬编码切到真实用户。
</success_criteria>

<output>
After completion, create `.planning/phases/13-zzz-prompt-debug-origin/13-03-SUMMARY.md`
</output>
