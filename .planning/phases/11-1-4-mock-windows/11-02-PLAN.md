---
phase: 11-1-4-mock-windows
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scripts/run-acceptance-smoke.ps1
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/11-1-4-mock-windows/11-VERIFICATION.md
  - .planning/phases/11-1-4-mock-windows/11-HUMAN-UAT.md
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-08
  - D-09
user_setup: []

must_haves:
  truths:
    - Acceptance smoke 会把 Phase 11 baseline 当作硬门禁，重置失败或 API 仍返回旧脏时间线时直接失败。
    - 运行中的后端接口会把 Jan-Apr 河南 baseline 暴露给环境数据页和预测归档，不需要回退任何前端 mock。
    - Phase 11 的规划与收口文档会明确说明本阶段只交付 mock 数据与脏数据清理，Windows 一键运行已延期。
  artifacts:
    - scripts/run-acceptance-smoke.ps1
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/11-1-4-mock-windows/11-VERIFICATION.md
    - .planning/phases/11-1-4-mock-windows/11-HUMAN-UAT.md
  key_links:
    - "`run-acceptance-smoke.ps1` 必须串起 `reset-demo-db.ps1` 与 `verify-demo-baseline.ps1`，把 Phase 11 baseline 变成自动化 blocking gate。"
    - "`/api/sensor-data`、`/api/sensor-data/trend`、`/api/grain-temp/summaries`、`/api/predictions/tasks` 必须都反映新的 Jan-Apr baseline，而不是旧 9 月 seed。"
    - "`ROADMAP.md` 与 `STATE.md` 必须同步记录“Windows 一键运行已延期”，避免后续执行者把 deferred 范围重新拉回 Phase 11。"
---

<objective>
把 Phase 11 的新 demo baseline 接进现有验收链路，并同步阶段文档，让执行完成后的验证证据和阶段边界都可以直接复用。

Purpose: 没有自动化 smoke 和文档收口，Phase 11 会重新退回“数据像是对了，但没人能快速证明”的状态。
Output: 带 Phase 11 baseline 断言的 `run-acceptance-smoke.ps1`，以及同步后的 roadmap / state / verification / human UAT 文档。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-1-4-mock-windows/11-CONTEXT.md
@.planning/phases/11-1-4-mock-windows/11-01-PLAN.md
@scripts/run-acceptance-smoke.ps1
@scripts/reset-demo-db.ps1
@scripts/verify-demo-baseline.ps1
@backend/src/main/java/com/grain/platform/controller/SensorDataController.java
@backend/src/main/java/com/grain/platform/service/SensorDataService.java
@frontend/src/api/grain.js
@frontend/src/views/DataView.vue

<interfaces>
From `backend/src/main/java/com/grain/platform/controller/SensorDataController.java`:

```java
@GetMapping
public ApiResponse<PageResult<SensorDataPointDto>> list(...)

@GetMapping("/trend")
public ApiResponse<SensorTrendResponse> trend(...)
```

From `frontend/src/api/grain.js`:

```javascript
export async function fetchSensorData(params = {}) { ... }
export async function fetchSensorTrend(params = {}) { ... }
export async function fetchPredictionTasks() { ... }
```

From `scripts/run-acceptance-smoke.ps1`:

```powershell
if (-not $SkipReset) {
    Invoke-ExternalCommand -Command $shellPath -Arguments @("-ExecutionPolicy", "Bypass", "-File", $resetScript)
}
```

Interpretation for Phase 11:
- smoke 不需要新 UI 自动化；只要用现有 REST API 断言新 baseline 已进入真实数据主链即可。
- Phase 11 的关键是“reset 后 API 是否读到新 baseline”，不是重新设计前端页面。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 Phase 11 baseline 校验接入 acceptance smoke 并补足 API 断言</name>
  <read_first>
    - scripts/run-acceptance-smoke.ps1
    - scripts/reset-demo-db.ps1
    - scripts/verify-demo-baseline.ps1
    - backend/src/main/java/com/grain/platform/controller/SensorDataController.java
    - backend/src/main/java/com/grain/platform/service/SensorDataService.java
    - frontend/src/api/grain.js
    - frontend/src/views/DataView.vue
  </read_first>
  <files>
    - scripts/run-acceptance-smoke.ps1
  </files>
  <action>
    1. 在 `run-acceptance-smoke.ps1` 的 reset 阶段之后增加对 `verify-demo-baseline.ps1` 的调用，把 Phase 11 baseline 变成启动后端前的 blocking gate；如果 baseline 校验失败，整个 smoke 必须立即停止。
    2. 在现有 dashboard / predictions / import smoke 之外，新增针对 Phase 11 的 API 断言：`/api/sensor-data` 分页返回目标仓库的 Jan-Apr 湿度或 co2 记录、`/api/sensor-data/trend` 返回连续趋势点、`/api/grain-temp/summaries` 返回 Jan-Apr 温度汇总、`/api/predictions/tasks` / 详情返回基于 Jan-Apr 真实数据训练并指向 Apr 30 之后预测窗的任务。
    3. 断言要显式检查“有新 baseline”与“没有旧 9 月主线残留”两件事；不要只做“列表非空”这种会让脏数据也通过的宽松检查。
    4. 保留已有用户 CRUD 和粮温导入 smoke，不要因为 Phase 11 新增断言而砍掉现有验收覆盖面。
  </action>
  <acceptance_criteria>
    - `run-acceptance-smoke.ps1` 现在会在 reset 后先验证 Phase 11 baseline，再进入后端启动与 API smoke。
    - sensor / grain summary / prediction archive 的 smoke 断言会明确证明接口已读到 Jan-Apr baseline，而不是仅验证接口可访问。
    - 一旦 reset 后仍留有旧 9 月 seed、prediction 时间窗未重写或 sensor_data 缺少 Phase 11 连续样本，smoke 会返回失败而不是误报通过。
  </acceptance_criteria>
  <verify>
    <automated>& '.\scripts\run-acceptance-smoke.ps1'</automated>
  </verify>
  <done>Phase 11 baseline 已纳入仓库既有 acceptance smoke 主链，后续任何回归都能自动暴露。</done>
</task>

<task type="auto">
  <name>Task 2: 同步 Phase 11 文档边界、验证证据与人工验收入口</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/11-1-4-mock-windows/11-CONTEXT.md
    - .planning/phases/11-1-4-mock-windows/11-01-PLAN.md
    - scripts/run-acceptance-smoke.ps1
  </read_first>
  <files>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/11-1-4-mock-windows/11-VERIFICATION.md
    - .planning/phases/11-1-4-mock-windows/11-HUMAN-UAT.md
  </files>
  <action>
    1. 先更新 `ROADMAP.md` 的 Phase 11 overview row，再同步 `STATE.md` 与 Phase 11 详细描述，明确本阶段实际范围是“河南 1-4 月 mock 数据 + demo 库重置清理”，并显式写出 Windows 一键运行 / 免安装依赖方案已延期到后续 phase；不要再保留 overview row 中“提供免安装依赖的一键运行方案”这类与 `11-CONTEXT.md` 冲突的旧占位文案。
    2. 新建 `11-VERIFICATION.md`，记录执行者实际跑过的 reset、baseline verifier、acceptance smoke 命令和结果摘要，并把关键 API 证据写成可复查条目。
    3. 新建 `11-HUMAN-UAT.md`，给出最小人工检查路径：DataView env 模式切换 humidity / co2 查看 Jan-Apr 趋势与分页、grain 模式查看 Jan-Apr 粮温汇总、PredictionView 查看与新 baseline 对齐的归档任务；不要要求用户再做手写 SQL。
    4. 文档中必须引用 D-01 至 D-09 的决策边界，并明确“Windows 一键运行 / 类 docker 免安装环境”仍是 deferred idea，不得被写回本 phase 的完成标准。
  </action>
  <acceptance_criteria>
    - `ROADMAP.md` 的 Phase 11 overview row 与 `STATE.md` 中的 Phase 11 范围已经与 `11-CONTEXT.md` 一致，且 overview row 不再把 Windows 一键运行写成当前 phase 交付物。
    - `11-VERIFICATION.md` 能让后续执行者直接复核 reset + smoke 的自动化证据。
    - `11-HUMAN-UAT.md` 能让用户在不查代码的情况下手工确认 Jan-Apr 温湿度/co2 与预测归档展示链路。
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; rg -n "Windows 一键运行 / 免安装依赖方案延期到后续 phase|Windows 一键运行已延期|1 月 1 日到 4 月 30 日|reset-demo-db|verify-demo-baseline|/api/sensor-data|/api/predictions/tasks|DataView|PredictionView" .planning/ROADMAP.md .planning/STATE.md .planning/phases/11-1-4-mock-windows/11-VERIFICATION.md .planning/phases/11-1-4-mock-windows/11-HUMAN-UAT.md; if (rg -n "免安装依赖的一键运行方案" .planning/ROADMAP.md) { Write-Error "Old contradictory Phase 11 overview wording still present in ROADMAP.md." }</automated>
  </verify>
  <done>Phase 11 的自动化验证证据、人工验收入口和实际阶段边界都已文档化，可直接用于收口和交接。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run-acceptance-smoke.ps1` → 已重置 demo 库与运行中的后端接口 | smoke 既要证明 reset 成功，也要证明 API 真正在消费新 baseline；任何宽松断言都会放过脏数据回归。 |
| Phase 文档 → 后续执行者 / 用户验收 | 如果文档继续带着旧 Roadmap 占位文本，后续执行者会把已延期的 Windows 一键运行重新误判成本阶段范围。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | S | `scripts/run-acceptance-smoke.ps1` | mitigate | 在 reset 后强制调用 `verify-demo-baseline.ps1`，并新增精确日期 / 仓库 / 预测窗口断言，避免“接口能通就算过”的伪成功。 |
| T-11-06 | R | `.planning/phases/11-1-4-mock-windows/11-VERIFICATION.md` | mitigate | 把执行过的命令、关键 API 结果和失败门槛写入 verification 文档，形成可追溯证据而不是口头结论。 |
| T-11-07 | T | `ROADMAP.md` / `STATE.md` | mitigate | 文档同步时显式写出 deferred 的 Windows packaging 范围，避免后续执行者根据旧占位文本重新扩大 Phase 11。 |
| T-11-08 | D | acceptance smoke 运行时长 | accept | 本阶段复用既有 smoke 主链，新增的是几条 API 断言与 baseline gate；运行成本增加可接受，优先保证 demo baseline 回归可检测。 |
</threat_model>

<verification>
- `& '.\scripts\run-acceptance-smoke.ps1'`
- `$ErrorActionPreference='Stop'; rg -n "Windows 一键运行 / 免安装依赖方案延期到后续 phase|Windows 一键运行已延期|1 月 1 日到 4 月 30 日|reset-demo-db|verify-demo-baseline|/api/sensor-data|/api/predictions/tasks|DataView|PredictionView" .planning/ROADMAP.md .planning/STATE.md .planning/phases/11-1-4-mock-windows/11-VERIFICATION.md .planning/phases/11-1-4-mock-windows/11-HUMAN-UAT.md; if (rg -n "免安装依赖的一键运行方案" .planning/ROADMAP.md) { Write-Error "Old contradictory Phase 11 overview wording still present in ROADMAP.md." }`
</verification>

<success_criteria>
- Acceptance smoke 会把 Phase 11 baseline 当作必须通过的门禁。
- 现有真实 API 已能自动证明 Jan-Apr 河南 baseline 可被环境数据页和预测归档链路消费。
- Phase 11 文档明确收口为“mock 数据 + 脏数据清理”，Windows 一键运行被清晰保留为 deferred work。
</success_criteria>

<output>
After completion, create `.planning/phases/11-1-4-mock-windows/11-02-SUMMARY.md`
</output>
