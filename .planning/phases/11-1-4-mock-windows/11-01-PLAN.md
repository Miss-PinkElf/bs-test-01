---
phase: 11-1-4-mock-windows
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/schema.sql
  - scripts/reset-demo-db.ps1
  - scripts/verify-demo-baseline.ps1
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
user_setup: []

must_haves:
  truths:
    - 运行 `scripts/reset-demo-db.ps1` 后，demo 库会回到同一份干净、可解释、可复位的标准演示状态，而不是混有旧脏数据。
    - 河南场景的 2025-01-01 到 2025-04-30 连续数据会覆盖 2 到 3 个重点仓库，其中温度继续走粮温链路，湿度与二氧化碳继续走 `sensor_data`。
    - 归档预测任务与结果会和新的 1-4 月真实温度基线保持同一条叙事，不再保留与旧 9 月样本相冲突的任务。
  artifacts:
    - backend/src/main/resources/db/schema.sql
    - scripts/reset-demo-db.ps1
    - scripts/verify-demo-baseline.ps1
  key_links:
    - "`schema.sql` 中 `sensor_data`、`grain_temp_record`、`grain_temp_summary`、`prediction_task`、`prediction_result` 的仓库与时间窗口必须保持同一套 Phase 11 baseline。"
    - "`reset-demo-db.ps1` 必须真正执行到标准 demo 库，而不是打印一个未生效的目标数据库名。"
    - "`verify-demo-baseline.ps1` 必须在重置后直接证明 Jan-Apr 连续数据存在、旧 9 月冲突样本已清除、预测任务时间窗与新基线一致。"
---

<objective>
把 Phase 11 的标准演示库基础一次性收口到 `schema.sql + reset-demo-db.ps1`：用可讲述的河南 1-4 月连续 mock 数据替换当前冲突 seed，并给 reset 路径补上可自动执行的基线校验。

Purpose: 先把“什么是干净 demo 库”定义清楚并做成一键可重建，否则后续 API / 页面验证都会建立在脏库或混合时间线之上。
Output: 新版 `schema.sql`、真实生效的 `reset-demo-db.ps1`、以及重置后可直接跑的 baseline 校验脚本。
</objective>

<execution_context>
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/workflows/execute-plan.md
@D:/Users/Mobius/Desktop/mine/AAA-code/bs-test-01/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-1-4-mock-windows/11-CONTEXT.md
@backend/src/main/resources/db/schema.sql
@scripts/reset-demo-db.ps1
@backend/src/main/java/com/grain/platform/service/PredictionService.java
@backend/src/main/java/com/grain/platform/service/GrainTempService.java
@frontend/src/views/DataView.vue
@zzz-docs/设计文档/数据库设计-滚动预测与高温预警版.md

<interfaces>
From `backend/src/main/java/com/grain/platform/service/PredictionService.java`:

```java
String dataSourceType = "temperature".equals(request.metricCode()) ? "GRAIN_TEMP_SUMMARY" : "SENSOR_DATA";

if ("temperature".equals(request.metricCode())) {
    return grainTempService.listPredictionSeries(
            request.warehouseId(),
            request.trainStartTime(),
            request.trainEndTime(),
            targetType
    );
}
```

From `frontend/src/views/DataView.vue`:

```javascript
const envMetricOptions = computed(() => metricOptions.value.filter((item) => item.value !== "temperature"));

const [sensorPage, trendList] = await Promise.all([
  fetchSensorData({ warehouseId: filters.warehouseId || undefined, metricCode: filters.metricCode, ... }),
  fetchSensorTrend({ warehouseId: filters.warehouseId || undefined, metricCode: filters.metricCode })
]);
```

Interpretation for Phase 11:
- 普通环境页的 env 模式只直接消费湿度 / 二氧化碳。
- 温度兼容性意味着要维持 `grain_temp_record` / `grain_temp_summary` / `prediction_task` 这条链路，而不是把温度硬塞回 env 指标下拉。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写标准演示库 seed，建立河南 1-4 月统一时间线</name>
  <read_first>
    - .planning/phases/11-1-4-mock-windows/11-CONTEXT.md
    - backend/src/main/resources/db/schema.sql
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/service/GrainTempService.java
    - frontend/src/views/DataView.vue
  </read_first>
  <files>
    - backend/src/main/resources/db/schema.sql
  </files>
  <action>
    1. 直接在 `schema.sql` 中重做 Phase 11 的标准 demo seed，严格按 D-01 / D-02 把“重置后标准演示库”定义为唯一真相源，不新增独立 mock 生成器或额外导入脚本。
    2. 复用现有重点仓库 ID，优先使用 `1 / 2 / 6` 形成“稳定仓 / 风险仓 / 对比仓”叙事，满足 D-05；不要新增一批新仓库把 phase 扩成结构改造。
    3. 把湿度与二氧化碳连续序列写入 `sensor_data`，时间窗口固定为 `2025-01-01 00:00:00` 到 `2025-04-30 23:59:59`，频率明确收口为“按天”或“按半天”之一；建议优先按天，以满足 D-06 的连续性同时控制 `schema.sql` 体量。
    4. 把温度数据保持在现有粮温链路上：写入与之匹配的 `grain_temp_record` 和 `grain_temp_summary`，保证 `PredictionService` 仍可从 `grain_temp_summary` 读取训练序列；不要把温度迁回 `sensor_data`。
    5. 删除或替换当前与新 baseline 冲突的旧 8-9 月 seed、脏 `quality_flag`、旧 remark 叙事和旧预测任务，使 `prediction_task` / `prediction_result` 重新围绕 Jan-Apr 真实温度、May 起预测时间窗建立一致故事线，落实 D-08 / D-09。
    6. 数据值必须体现河南 1-4 月季节抬升的可讲述趋势：稳定仓波动平缓，风险仓在 4 月末更接近阈值，对比仓保留一定波动但不复用旧“修正明显仓”的 9 月高温故事；禁止写成随机数风格的跳点。
  </action>
  <acceptance_criteria>
    - `schema.sql` 中 `sensor_data`、`grain_temp_record`、`grain_temp_summary`、`prediction_task`、`prediction_result` 的 seed 时间窗已统一收口到 Phase 11 的 Jan-Apr baseline，不再保留旧 9 月演示主线。
    - 温度 seed 继续通过 `grain_temp_*` 表支撑图表与预测，湿度 / co2 只通过 `sensor_data` 支撑 env 模式，和当前代码契约一致。
    - 选定仓库与 remark / warning 叙事能直接支持“稳定仓 / 风险仓 / 对比仓”的答辩讲述，不需要执行时再补口径解释。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "INSERT INTO sensor_data|INSERT INTO grain_temp_record|INSERT INTO grain_temp_summary|INSERT INTO prediction_task|INSERT INTO prediction_result|2025-01-|2025-04-|2025-05-" backend/src/main/resources/db/schema.sql</automated>
  </verify>
  <done>`schema.sql` 已独立定义出一份自洽的 Phase 11 标准演示库种子，覆盖 Jan-Apr 河南环境与温度/预测链路。</done>
</task>

<task type="auto">
  <name>Task 2: 收口 reset-demo-db 并新增重置后 baseline 自动校验</name>
  <read_first>
    - scripts/reset-demo-db.ps1
    - backend/src/main/resources/db/schema.sql
    - .planning/phases/11-1-4-mock-windows/11-CONTEXT.md
  </read_first>
  <files>
    - scripts/reset-demo-db.ps1
    - scripts/verify-demo-baseline.ps1
  </files>
  <action>
    1. 修正 `reset-demo-db.ps1` 的目标数据库语义，使其输出的 target 与实际被 `schema.sql` 重建的 demo 库一致；不要继续保留“打印了 `$Database` 参数，但实际完全由 `schema.sql` 的 `USE grain_env_predict` 决定”的误导行为。
    2. 新增 `scripts/verify-demo-baseline.ps1`，用 mysql CLI 在 reset 之后直接断言以下事实：目标仓库的 `sensor_data` 只包含 Phase 11 允许的温湿度/co2 Jan-Apr baseline；`grain_temp_summary` 和 `grain_temp_record` 覆盖同一时间窗；`prediction_task` / `prediction_result` 的训练与预测窗口和新 baseline 一致；旧 9 月冲突样本已被清除。
    3. 校验脚本要输出可读的通过 / 失败原因，并在任一断言失败时返回非零退出码，让后续 smoke 可以把它当作 blocking gate；不要让执行者再手写临时 SQL 查询。
    4. 所有校验都围绕 D-02 / D-08 / D-09 的“可重建干净 demo 库”定义展开，不额外引入 UI 入口、后台清洗接口或复杂清洗规则。
  </action>
  <acceptance_criteria>
    - 执行者只需运行 reset 和 verifier 两个脚本，就能判断 demo 库是否已回到标准 Phase 11 baseline。
    - `reset-demo-db.ps1` 不再对目标数据库产生歧义；脚本的失败信息能够指向 schema 路径、mysql CLI 缺失或 baseline 断言失败中的具体原因。
    - `verify-demo-baseline.ps1` 会明确检查“存在 Jan-Apr 连续数据”和“不存在旧 9 月主线残留”这两类条件。
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; & '.\scripts\reset-demo-db.ps1'; & '.\scripts\verify-demo-baseline.ps1'</automated>
  </verify>
  <done>Phase 11 的标准 demo 库已经能被脚本稳定重建，并且能在不启动前后端的情况下自动证明 baseline 正确。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `schema.sql` → MySQL demo 库 | 本阶段把标准演示数据直接固化到 SQL seed；任何时间窗或仓库选择错误都会直接污染重置后的真相源。 |
| PowerShell reset / verify scripts → 本地 `mysql` CLI | 本地脚本负责重置和断言 baseline，错误的参数语义或宽松断言会让脏库伪装成“重置成功”。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | `backend/src/main/resources/db/schema.sql` | mitigate | 用明确的仓库 ID、Jan-Apr 时间窗和旧 9 月冲突样本清理规则重写 seed，并在 `verify-demo-baseline.ps1` 中逐表断言日期范围与记录存在性。 |
| T-11-02 | S | `scripts/reset-demo-db.ps1` | mitigate | 让 reset 脚本的目标数据库语义与 `schema.sql` 真正一致，避免“口头目标库”和实际重建库不一致造成伪成功。 |
| T-11-03 | I | PowerShell 输出与日志 | mitigate | 继续沿用现有脚本的 host/user 输出方式，不把密码、整段 SQL 或敏感连接细节打印到日志中。 |
| T-11-04 | D | 大体量 seed 导入 | mitigate | 将仓库数量控制在 2-3 个并优先按天采样，满足 D-05 / D-06 的连续性要求，同时避免重置脚本因过量 seed 明显变慢。 |
</threat_model>

<verification>
- `rg -n "INSERT INTO sensor_data|INSERT INTO grain_temp_record|INSERT INTO grain_temp_summary|INSERT INTO prediction_task|INSERT INTO prediction_result|2025-01-|2025-04-|2025-05-" backend/src/main/resources/db/schema.sql`
- `$ErrorActionPreference='Stop'; & '.\scripts\reset-demo-db.ps1'; & '.\scripts\verify-demo-baseline.ps1'`
</verification>

<success_criteria>
- Phase 11 的标准演示库已经完全由 `schema.sql` 定义，且不会再和旧 9 月样本混用。
- reset 脚本与 baseline 校验脚本可以在本地直接证明 demo 库重建成功。
- 温度链路继续兼容 `grain_temp_summary -> PredictionService`，湿度与二氧化碳继续兼容 `sensor_data -> DataView env`。
</success_criteria>

<output>
After completion, create `.planning/phases/11-1-4-mock-windows/11-01-SUMMARY.md`
</output>
