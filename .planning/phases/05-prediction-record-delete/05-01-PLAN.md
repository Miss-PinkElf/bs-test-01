---
phase: 05-prediction-record-delete
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/grain/platform/dto/prediction/PredictionBatchDeleteRequest.java
  - backend/src/main/java/com/grain/platform/controller/PredictionController.java
  - backend/src/main/java/com/grain/platform/service/PredictionService.java
  - backend/src/main/java/com/grain/platform/mapper/PredictionTaskMapper.java
  - backend/src/main/java/com/grain/platform/mapper/PredictionResultMapper.java
  - backend/src/main/resources/mapper/PredictionTaskMapper.xml
  - backend/src/main/resources/mapper/PredictionResultMapper.xml
  - frontend/src/api/grain.js
  - frontend/src/views/PredictionView.vue
autonomous: true
requirements:
  - DEL-05-01
  - DEL-05-02
user_setup: []

must_haves:
  truths:
    - 预测记录表最左侧存在 selection 多选列；工具栏存在「批量删除」danger 按钮；无勾选时按钮禁用（或等价一致提示策略）
    - 操作列在「查看摘要」「切换任务」外存在「删除」；单删与批量删调用接口前均有 ElMessageBox.confirm；批量确认文案含选中数量
    - 后端提供 DELETE /api/predictions/tasks/{taskId} 与 POST /api/predictions/tasks/batch-delete（body 含 taskIds）；删除 prediction_result 与 prediction_task；批量在单事务内
    - 删除成功后刷新列表并清空表格多选；若删除集合包含当前 selectedTaskId 则清空 prediction 展示与图表
    - 前端 API 封装于 grain.js，PredictionView 无硬编码完整 URL
  artifacts:
    - frontend/src/views/PredictionView.vue
    - frontend/src/api/grain.js
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
  key_links:
    - .planning/phases/05-prediction-record-delete/05-CONTEXT.md
    - .planning/phases/05-prediction-record-delete/05-UI-SPEC.md
---

<objective>
落实 Phase 5：预测记录 **表前多选**、**批量删除**、操作列 **单条删除**；**凡删除二次确认**（批量带数量）；后端 **单删 + 批量删** API 与持久化一致性。
</objective>

<execution_context>
@E:/Learn/Vs/Code/bs-test-01/.cursor/get-shit-done/workflows/execute-plan.md
@E:/Learn/Vs/Code/bs-test-01/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prediction-record-delete/05-CONTEXT.md
@.planning/phases/05-prediction-record-delete/05-UI-SPEC.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@frontend/src/views/PredictionView.vue
@frontend/src/api/grain.js
@backend/src/main/java/com/grain/platform/service/PredictionService.java
@backend/src/main/java/com/grain/platform/controller/PredictionController.java
@backend/src/main/resources/mapper/PredictionTaskMapper.xml
@backend/src/main/resources/mapper/PredictionResultMapper.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端单删与批量删（D-01～D-03b）</name>
  <read_first>
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/resources/mapper/PredictionTaskMapper.xml
    - backend/src/main/resources/mapper/PredictionResultMapper.xml
    - .planning/phases/05-prediction-record-delete/05-CONTEXT.md
  </read_first>
  <files>
    - backend/src/main/java/com/grain/platform/mapper/PredictionResultMapper.java
    - backend/src/main/java/com/grain/platform/mapper/PredictionTaskMapper.java
    - backend/src/main/resources/mapper/PredictionResultMapper.xml
    - backend/src/main/resources/mapper/PredictionTaskMapper.xml
    - backend/src/main/java/com/grain/platform/service/PredictionService.java
    - backend/src/main/java/com/grain/platform/controller/PredictionController.java
    - backend/src/main/java/com/grain/platform/dto/prediction/PredictionBatchDeleteRequest.java
  </files>
  <action>
    1. Mapper：为 `prediction_result` 增加按 `task_id` 删除（单 taskId）；为 `prediction_task` 增加 `deleteById`；批量场景可先按 taskId 列表循环删子表再删父表，或增加 `deleteByIds` / `deleteResultsByTaskIds`，保持单事务。
    2.新建请求 DTO（如 `PredictionBatchDeleteRequest` record：`List&lt;Long&gt; taskIds`），`taskIds` 空或 null 时返回 400。
    3. `PredictionService`：`deleteTask(Long id)`、`deleteTasksBatch(List&lt;Long&gt; ids)`，内联调用现有 `getTask`/`selectById` 校验存在性；批量推荐「任一不存在则整批失败回滚」与 CONTEXT D-02 一致。
    4. `PredictionController`：`@DeleteMapping("/tasks/{taskId}")`；`@PostMapping("/tasks/batch-delete")` + `@RequestBody` DTO；日志与 `ApiResponse` 风格对齐现有方法。
  </action>
  <acceptance_criteria>
    - `rg "batch-delete|DeleteMapping.*tasks" backend/src/main/java/com/grain/platform/controller/PredictionController.java` 能体现单删与批量路由。
    - Mapper XML 或注解含对 result / task 的 delete 语句。
  </acceptance_criteria>
  <verify>
    <automated>cd backend && mvn -q -DskipTests compile</automated>
  </verify>
  <done>后端删除接口可编译。</done>
</task>

<task type="auto">
  <name>Task 2: 前端 API 封装</name>
  <read_first>
    - frontend/src/api/grain.js（fetchPredictionTasks、request 模式）
    - .planning/phases/05-prediction-record-delete/05-CONTEXT.md
  </read_first>
  <files>frontend/src/api/grain.js</files>
  <action>
    1. 新增 `deletePredictionTask(taskId)` → DELETE `/api/predictions/tasks/{taskId}`。
    2. 新增 `batchDeletePredictionTasks(taskIds)` → POST `/api/predictions/tasks/batch-delete`，body `{ taskIds }`。
    3. 错误处理与现有 `request`/`ApiResponse` 解析一致。
  </action>
  <acceptance_criteria>
    - `rg "batch-delete|deletePredictionTask" frontend/src/api/grain.js` 命中封装函数。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>API 封装完成且构建通过。</done>
</task>

<task type="auto">
  <name>Task 3: PredictionView 多选、批量删、单删与状态清理（D-04～D-07）</name>
  <read_first>
    - frontend/src/views/PredictionView.vue
    - .planning/phases/05-prediction-record-delete/05-UI-SPEC.md
  </read_first>
  <files>frontend/src/views/PredictionView.vue</files>
  <action>
    1. 引入 `ElMessageBox`；表格首列 `type="selection"`，`ref` 便于清空 selection。
    2. 工具栏「批量删除」：`danger`，无选中行时 `disabled`（推荐）。
    3. 批量：`confirm` 文案含 **选中数量** → `batchDeletePredictionTasks` → 成功则 `loadPredictionHistory`、**clearSelection**、按 D-07 判断是否清空 `prediction`/`selectedTaskId`/图表。
    4. 操作列「删除」：`confirm` → `deletePredictionTask` → 同上刷新与状态处理。
    5. 操作列宽度若三枚按钮拥挤则略增 `width`（Claude discretion）。
  </action>
  <acceptance_criteria>
    - `rg "type=\"selection\"|batch-delete|ElMessageBox|批量删除|deletePredictionTask" frontend/src/views/PredictionView.vue` 合理命中。
    - 无删除请求在用户取消 confirm 后发出（逻辑审查）。
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>前端交互落地且构建通过。</done>
</task>

<task type="auto">
  <name>Task 4: GSD 与需求追溯同步</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/05-prediction-record-delete/05-VERIFICATION.md
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/05-prediction-record-delete/05-VERIFICATION.md
  </files>
  <action>
    1. 确认 REQUIREMENTS 已含 DEL-05-*；ROADMAP Phase 5 Goal / Success / Plans 与 `05-01-PLAN.md` 勾选一致。
    2. 更新 `05-VERIFICATION.md`：列出 must_haves 对照方式（实现后填证据）。
    3. STATE.md 追加 Phase 5 plan 落盘与执行状态一行。
  </action>
  <acceptance_criteria>
    - `rg "DEL-05" .planning/REQUIREMENTS.md` 命中。
    - ROADMAP Phase 5 下 Plans 含 `05-01-PLAN.md` 条目。
  </acceptance_criteria>
  <verify>
    <automated>rg "DEL-05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>文档与计划一致。</done>
</task>

</tasks>

<threat_model>
## Trust 边界

| 边界 | 说明 |
|------|------|
| 浏览器 → API | 管理端会话内；删除为破坏性操作，依赖既有认证与同源策略。 |

## STRIDE 登记

| Threat ID | 类别 | 组件 | 处置 | 缓解 |
|-----------|------|------|------|------|
| T-05-01 | T | 删除接口 | 缓解 | 仅认证用户可调用；二次确认在前端降低误触；服务端校验 id。 |
| T-05-02 | I | 批量删除 | 缓解 | 服务端校验 taskIds 非空；事务避免部分成功误导。 |
</threat_model>

<verification>
## 自动化

- `mvn -DskipTests compile`（backend）
- `npm run build`（frontend）

## 手工建议

- 勾选多条批量删除：确认框条数正确；取消不发请求。
- 删除当前查看任务：图表与摘要清空。
- 单条删除：同上。
</verification>
