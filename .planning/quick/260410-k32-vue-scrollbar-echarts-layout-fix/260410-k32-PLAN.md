# Quick Plan: 260410-k32

**Mode:** quick（补录 — 修复已在对话中完成并经用户确认有效）  
**Date:** 2026-04-10  
**Description:** Vue 管理端温度预测等页进入后横向宽度持续增长；根因定位为 `el-scrollbar` 与 ECharts 的 ResizeObserver / 更新链互相反馈；主内容区改原生滚动并收紧 flex /图表 resize。

## Tasks

### Task 1 — 主内容滚动容器（`ConsoleLayout.vue` + `styles.css`）

- **Files:** `frontend/src/layout/ConsoleLayout.vue`、`frontend/src/styles.css`
- **Action:** 去掉主内容区 `el-scrollbar`，改为 `div.console-main-native`，使用 `overflow-y: auto`、`overflow-x: hidden`、`min-width: 0`、`scrollbar-gutter: stable`；`.console-body` / `.console-main` 使用 `flex: 1 1 0`；顶栏与 `.console-header` 子项补充 `min-width: 0` / `flex-shrink: 0`；小屏媒体查询同步指向新类名。
- **Verify:** `npm run build`（`frontend/`）
- **Done:** 是

### Task 2 — 预测页图表尺寸（`PredictionView.vue`）

- **Files:** `frontend/src/views/PredictionView.vue`
- **Action:** `setOption` 后于 `requestAnimationFrame` 内调用 `chart.resize({ width: clientWidth, height: clientHeight, animation: { duration: 0 } })`，避免与布局抖动叠加。
- **Verify:** 浏览器进入 `/prediction` 横向不再持续扩张（与用户确认）
- **Done:** 是

### Task 3 — GSD quick + devflow 记录

- **Files:** `.planning/STATE.md`、`.planning/quick/260410-k32-vue-scrollbar-echarts-layout-fix/*`、`.devflow/grain-platform-bootstrap/bug-log.md`、`.devflow/grain-platform-bootstrap/learnings.md`、`.devflow/grain-platform-bootstrap/state.md`
- **Action:** 补录 PLAN/SUMMARY；STATE「Quick Tasks Completed」增行；devflow 新建 `bug-log.md` 条目并更新 `learnings.md`；`state.md` 已确认事实增一条并指向 bug/learning。
- **Verify:** 路径与 quick_id 一致、与 devflow 无矛盾
- **Done:** 是

---

*PLANNING COMPLETE — 路径：`.planning/quick/260410-k32-vue-scrollbar-echarts-layout-fix/260410-k32-PLAN.md`*
