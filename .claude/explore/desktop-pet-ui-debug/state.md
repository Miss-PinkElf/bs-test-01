# 当前状态

## 当前阶段
前端边框联动缩放专项调试已完成，当前作为专项归档保留

## 已确认的事实
- `PIXI.Application` 单例 + renderer.resize 的修复方案已经保留，当前这轮不能回退。
- 当前新问题不是生命周期，而是边框、模型、toolbar 没有共享同一套实时尺寸源和布局基准。
- 真实窗口 resize 之前没有稳定同步到 React 侧 `windowBounds`，导致“边框变了，但模型和 toolbar 看起来没怎么变”。
- 模型与边框空白过多，不只是 fit 比例偏保守，还因为缺少统一的模型可视区定义。

## 工作假设
- 让 React 订阅真实窗口 `resize`，并把模型绘制区域从整窗改成统一可视区后，模型和 toolbar 的联动会明显改善。
- 若模型留白仍偏大，后续优先继续微调 `MODEL_FILL_RATIO`、可视区 inset 和垂直偏移，而不是回退 PIXI 方案。

## 待解决的问题
- 本专项内的核心问题已完成验证。
- 若未来出现新的前端交互或视觉异常，再基于本目录继续追加专项记录。

## 下一步
- 将毕设主线切换到 `.claude/explore/desktop-pet-graduation-roadmap/`
- 用 superspec 推进“简单对话 + 后端最小闭环”
- 当前目录仅作为前端专项调试归档保留

## 最小活跃上下文摘要
这一轮前端专项已经完成，核心结论是：保持 `PIXI.Application` 单例、统一缩放基准、用统一模型可视区重算缩放。后续毕设推进请以全局 roadmap 目录为主。
