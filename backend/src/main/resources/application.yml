server:
  port: 8080

spring:
  application:
    name: grain-platform-backend
  datasource:
    url: jdbc:mysql://localhost:3306/grain_env_predict?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.grain.platform.entity
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
