# Phase 9 交付归档（2026-04-11）

## 主题

管理端列表去 mock，并收口 Users / Warehouse / Prediction / Dashboard 到服务端分页查询。

## 本次交付

- Users：新增后端分页与统计接口，页面切到服务端 `keyword + pageNum + pageSize`。
- Warehouse：新增后端分页与统计接口，页面切到服务端分页并保留列表驱动详情。
- Prediction：新增完整详情分页接口，页面切到服务端分页，查看摘要 / 切换任务 / 单删 / 批量删交互不回退。
- Dashboard：拆分为 `overview + alerts + warehouse-health + grain-summaries` 三块独立分页模块。
- Mock 清理：删除 `frontend/src/mock/platform.js`，不再以本地假数据作为运行时兜底。

## 验证结果

- 后端编译：通过
- 前端构建：通过
- 人工验收：通过

## 归档文件

- `.planning/phases/09-mock/09-01-SUMMARY.md`
- `.planning/phases/09-mock/09-02-SUMMARY.md`
- `.planning/phases/09-mock/09-VERIFICATION.md`
- `.planning/phases/09-mock/09-HUMAN-UAT.md`

## 备注

Phase 9 已确认可接受，现已从“待 UAT”收口为完成状态。后续如需继续扩展管理端高级筛选、Prediction 轻量列表 DTO 或 Dashboard 更细粒度统计，可作为新 phase 单独规划。
