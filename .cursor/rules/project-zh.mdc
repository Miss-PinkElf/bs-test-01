---
description: 项目协作约束与开发规范（简体中文、devflow、提交与代码风格）
alwaysApply: true
---

# 协作约束

## 工具约束

## 1. 输出与语言

1. 必须始终使用简体中文。
2. 产出的文档默认使用简体中文，禁止默认输出英文。
3. 包括但不限于 devflow、OpenSpec、handoff、skills 相关文档，默认都使用简体中文。

## 2. 提交流程

1. 每次完成代码修改后，必须先询问我是否需要提交代码。
2. 没有我的明确允许，不能执行 commit。
3. 如果需要 commit，提交信息必须使用中文。

## 3. 工作流要求（含「实现前对齐」硬约束）

### 3.1 总原则

1. 默认使用 `devflow` 进行长期任务的探索、记录和推进。
2. 在走 `devflow` 流程时，需要按阶段进入其中的 OpenSpec / Superpowers 子技能完成对齐、计划、实施、验证与 handoff。
3. 长期任务或跨多轮任务：真相源以 `.devflow/<mission-slug>/state.md` 与最新 handoff 为准；**进入 Plan / Apply 前**至少做一次 Mini Align（目标、范围、不做啥）。

### 3.2 何时必须先对齐、禁止直接改代码

凡用户请求属于下列任一情形，**默认视为「需要先对齐方案」**，**禁止**在同一条回复里直接开始批量改文件（含大范围 `apply_patch`、多文件重构、新接口/新页面主流程），**除非**用户在同轮对话中明确写了类似：**「直接做」「不用讨论」「按你上一条方案实现」** 等豁免语。

- 新增或明显改变**产品行为**（新功能、新交互、新页面、改业务流程）。
- 涉及**多个模块/多个文件**，或同时改**前端 + 后端**。
- 涉及 **API / 数据库 / 权限 / 安全**。
- 需求本身仍有歧义、缺少边界（「优化一下」「像 CRUD 那样」等未限定范围）。

**反例（禁止）：** 用户只说「加个搜索」，助手不说明字段范围与前后端分工就直接改十几处文件。

### 3.3 对齐阶段助手**必须**先给出的内容（再谈写代码）

在满足 3.2 时，本条回复内**必须先**包含下面三块（可短写，但必须可核对）：

1. **理解**：用一两句话复述目标与范围；列出**不做**或**待定**项（若有）。
2. **方案**：推荐一种实现路径；若有多选，说明取舍（影响面、风险、是否动后端）。
3. **待你确认**：明确问一句让用户选（例如「是否采纳 A」「是否允许动接口」），或列出需要你拍板的选项。

在用户针对方案给出明确同意或补充约束之前，**只做**读代码、查文档、列文件清单、更新 `.devflow/` 下讨论记录（若用户允许落盘）；**不进入**实质性编码。

### 3.4 头脑风暴与技能

1. 在 3.2 情形下，实现前须进行**头脑风暴式对齐**（可与用户多轮短问），必要时按仓库 skill 调用 `brainstorming` / `devflow` 子技能中的对齐说明执行。
2. 需要顺手判断：这次需求是否需要补充或更新相关文档（含 `.devflow` 内 state/plan）。

## 4. 文档与计划落盘要求

1. plan 文件名必须与本次需求强相关，便于后续查找。
2. 走 `devflow` 时，当前 mission 的 plan / spec / handoff / state / checkpoint 等过程文档，必须优先落在 `.devflow/<mission-slug>/` 下，不能默认写到 `docs/superpowers/`。
3. 只有在我明确要求产出仓库级公共文档时，才可以写到 `docs/` 或其它公共目录。
4. 非 `devflow` 的普通说明文档，除特殊说明外，再按现有约定放到对应目录，不要擅自混放。
5. 更新问题清单时，必须写清楚：
   - 问题现象
   - 问题原因
   - 解决方案

## 5. 路径与环境要求

1. 本仓库内涉及文件路径时，必须使用相对路径。

## 6. 校验与改动边界

1. 不需要做全局 ESLint 校验。
2. 不影响运行的 TypeScript 报错可以先不处理。
3. 如果要顺手修改 TypeScript 错误，必须先征求我的确认。

---

# 开发规范

## 1. 代码风格

1. 可读性优先。
2. 修改 React / TSX 代码时，优先参考现有代码风格保持一致。
3. 需要使用 `react-tsx-readability-guard` 提升 React / TSX 代码可读性。

## 2. 组件库

1. 使用 element-plus。

## 3. 样式规范

1. 默认不要使用行内样式。
2. 默认使用 CSS Module，推荐 `less` 或 `sass`。
3. 样式结构默认采用「外层包裹 + 内层 className」的写法。

### CSS Module 示例

```css
.wrapper {
  padding: 20px;
  background: #f5f5f5;

  :global {
    .user-info {
      .user-name {
      }
    }
  }
}
```

```tsx
<div className={styles.wrapper}>
  <div className="user-info">
    <span className="user-name">张三</span>
  </div>
</div>
```

### CSS-in-JS 示例

```tsx
export const DetailDiv = styled.div`
  .user-info {
    .user-name {
    }
  }
`;

export function Example() {
  return (
    <DetailDiv>
      <div className="user-info">
        <div className="user-name" />
      </div>
    </DetailDiv>
  );
}
```
